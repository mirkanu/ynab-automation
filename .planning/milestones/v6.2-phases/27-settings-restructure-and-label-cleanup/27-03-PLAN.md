---
phase: 27-settings-restructure-and-label-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 27-02
files_modified:
  - src/app/(dashboard)/settings/page.tsx
  - src/app/(dashboard)/tools/page.tsx
autonomous: true
requirements:
  - NAV-02
must_haves:
  truths:
    - "Tools page shows the Test Mode toggle"
    - "Settings page does not show the Test Mode toggle"
    - "Test Mode toggle on Tools page reads live testMode value from DB"
  artifacts:
    - path: "src/app/(dashboard)/tools/page.tsx"
      provides: "Tools page with SettingsForm (test mode toggle) and TestParseForm"
      exports: ["default ToolsPage"]
    - path: "src/app/(dashboard)/settings/page.tsx"
      provides: "Settings page with only API keys, YNAB connection, admin password"
  key_links:
    - from: "src/app/(dashboard)/tools/page.tsx"
      to: "SettingsForm"
      via: "import from ../settings/SettingsForm"
      pattern: "SettingsForm"
    - from: "src/app/(dashboard)/tools/page.tsx"
      to: "getSetting('TEST_MODE')"
      via: "server component DB read"
      pattern: "getSetting"
---

<objective>
Move the Test Mode toggle (SettingsForm) from the Settings page to the Tools page.

Purpose: Test Mode is an operational toggle used when testing email parsing, so it belongs alongside the parse/replay tools rather than mixed into credential management. After plan 02 the Settings page has only credentials and connections; this plan completes the restructure.
Output: SettingsForm rendered in tools/page.tsx, removed from settings/page.tsx.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Existing component interface the executor needs. -->

From src/app/(dashboard)/settings/SettingsForm.tsx:
```typescript
interface SettingsFormProps {
  testMode: boolean
}
// Usage: <SettingsForm testMode={testMode} />
// 'use client' component — toggle calls /api/settings endpoint internally
```

Current tools/page.tsx:
- Sync function (no async) — reads SENDERS from process.env (legacy fallback, can remain)
- Renders <TestParseForm defaultSenderName={defaultSenderName} />

Current settings/page.tsx (after plan 02):
- Still imports SettingsForm and reads testMode via getSetting('TEST_MODE')
- Still renders <SettingsForm testMode={testMode} />
- testMode read and SettingsForm import will be removed in this plan
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Test Mode toggle to Tools page and remove it from Settings page</name>
  <files>src/app/(dashboard)/tools/page.tsx, src/app/(dashboard)/settings/page.tsx</files>
  <action>
    In src/app/(dashboard)/tools/page.tsx:
    - Convert to async server component: `export default async function ToolsPage()`
    - Add imports: `import { getSetting } from '@/lib/settings'` and `import SettingsForm from '../settings/SettingsForm'`
    - Add at top of function body: `const testModeValue = await getSetting('TEST_MODE')` and `const testMode = testModeValue === 'true'`
    - Keep the existing SENDERS env read and defaultSenderName logic exactly as-is
    - Add <SettingsForm testMode={testMode} /> ABOVE <TestParseForm> in the JSX (test mode toggle comes first, then the parse tools)
    - Update the page subtitle to: "Toggle test mode and test email parsing or replay transactions."

    In src/app/(dashboard)/settings/page.tsx:
    - Remove the import for SettingsForm: `import SettingsForm from './SettingsForm'`
    - Remove `const testModeValue = await getSetting('TEST_MODE')` and `const testMode = testModeValue === 'true'`
    - Remove the JSX comment "1. Email Processing (test mode toggle)" and `<SettingsForm testMode={testMode} />` element
    - Keep getSetting import (still used for testMode read in layout — wait, layout does its own read, so check if getSetting is used elsewhere in settings/page.tsx after this change)
    - After removing testMode reads: if getSetting is no longer called anywhere in settings/page.tsx, remove that import too. If it is still used (e.g. YNAB_BUDGET_ID was removed in plan 02), then remove the getSetting import entirely since no calls remain.
    - Keep all other sections: ApiKeysSection, YnabConnectionSection, AdminPasswordSection
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - tools/page.tsx imports and renders SettingsForm with live testMode from DB
    - settings/page.tsx has no SettingsForm import or JSX
    - TypeScript compiles without errors
    - No unused imports remain in either file
  </done>
</task>

</tasks>

<verification>
- grep -n "SettingsForm" src/app/(dashboard)/settings/page.tsx — zero matches
- grep -n "SettingsForm" src/app/(dashboard)/tools/page.tsx — shows import and JSX usage
- grep -n "getSetting" src/app/(dashboard)/settings/page.tsx — zero matches (all reads removed after plan 02 + plan 03)
- npx tsc --noEmit — zero errors
</verification>

<success_criteria>
- Tools page renders Test Mode toggle above the parse form
- Settings page renders only: ApiKeysSection, YnabConnectionSection, AdminPasswordSection
- No orphaned imports in either file
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/27-settings-restructure-and-label-cleanup/27-03-SUMMARY.md`
</output>
