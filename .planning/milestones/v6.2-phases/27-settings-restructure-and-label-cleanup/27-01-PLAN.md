---
phase: 27-settings-restructure-and-label-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/setup/3/page.tsx
  - src/app/setup/done/page.tsx
autonomous: true
requirements:
  - LABEL-01
must_haves:
  truths:
    - "Wizard step 3 uses 'transactions' instead of 'Amazon transactions'"
    - "Setup done page uses 'order confirmation email' instead of 'Amazon order confirmation email'"
  artifacts:
    - path: "src/app/setup/3/page.tsx"
      provides: "Wizard step 3 with generic wording"
      contains: "transactions in"
    - path: "src/app/setup/done/page.tsx"
      provides: "Setup done page with generic wording"
      contains: "order confirmation email"
  key_links:
    - from: "src/app/setup/3/page.tsx"
      to: "user-facing copy"
      via: "JSX string literal"
      pattern: "transactions in"
    - from: "src/app/setup/done/page.tsx"
      to: "user-facing copy"
      via: "JSX string literal"
      pattern: "order confirmation email"
---

<objective>
Replace Amazon-specific language in wizard step 3 and the setup done page with generic wording.

Purpose: The app works with any retailer, not just Amazon. Step 3 still says "Amazon transactions" and setup/done says "Amazon order confirmation" — both mislead users who forward non-Amazon receipts.
Output: Two files with string replacements; no logic changes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Amazon-specific strings in wizard step 3 and setup/done</name>
  <files>src/app/setup/3/page.tsx, src/app/setup/done/page.tsx</files>
  <action>
    In src/app/setup/3/page.tsx line ~221:
    Change: "Tell the app which YNAB budget and account to create Amazon transactions in."
    To:     "Tell the app which YNAB budget and account to create transactions in."

    In src/app/setup/done/page.tsx line ~74:
    Change: "Forward an Amazon order confirmation email to"
    To:     "Forward an order confirmation email to"

    These are the only two occurrences. No logic, imports, or structure changes needed.
  </action>
  <verify>
    <automated>grep -n "Amazon" /data/home/ynab/src/app/setup/3/page.tsx /data/home/ynab/src/app/setup/done/page.tsx || echo "No Amazon strings found — PASS"</automated>
  </verify>
  <done>Neither file contains the word "Amazon" in any user-visible string.</done>
</task>

</tasks>

<verification>
grep -r "Amazon" src/app/setup/ — should return zero matches in user-visible strings (comments and variable names are acceptable if any exist, but the JSX copy must be clean).
</verification>

<success_criteria>
- src/app/setup/3/page.tsx: "transactions in" replaces "Amazon transactions in"
- src/app/setup/done/page.tsx: "order confirmation email" replaces "Amazon order confirmation email"
- No other changes to these files
</success_criteria>

<output>
After completion, create `.planning/phases/27-settings-restructure-and-label-cleanup/27-01-SUMMARY.md`
</output>
