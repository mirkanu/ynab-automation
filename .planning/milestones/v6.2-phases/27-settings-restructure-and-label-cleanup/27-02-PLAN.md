---
phase: 27-settings-restructure-and-label-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/settings/page.tsx
  - src/app/(dashboard)/rules/page.tsx
  - src/app/(dashboard)/rules/loading.tsx
autonomous: true
requirements:
  - NAV-01
must_haves:
  truths:
    - "Nav bar shows 5 items: Dashboard, Activity Log, Rules, Settings, Tools"
    - "Visiting /rules shows SenderRulesSection and CurrencyRulesSection"
    - "Visiting /settings no longer shows SenderRulesSection or CurrencyRulesSection"
    - "/rules has a skeleton loading state"
  artifacts:
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Nav bar with Rules link"
      contains: "/rules"
    - path: "src/app/(dashboard)/rules/page.tsx"
      provides: "Rules page with sender and currency routing sections"
      exports: ["default RulesPage"]
    - path: "src/app/(dashboard)/rules/loading.tsx"
      provides: "Skeleton loading state for rules page"
      exports: ["default RulesLoading"]
    - path: "src/app/(dashboard)/settings/page.tsx"
      provides: "Settings page without routing rule sections"
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "/rules"
      via: "anchor href"
      pattern: "href=\"/rules\""
    - from: "src/app/(dashboard)/rules/page.tsx"
      to: "SenderRulesSection, CurrencyRulesSection"
      via: "import from ../settings/"
      pattern: "SenderRulesSection|CurrencyRulesSection"
---

<objective>
Add a "Rules" nav item and page that hosts SenderRulesSection and CurrencyRulesSection, and remove those sections from the Settings page.

Purpose: The Settings page currently mixes credentials (API keys, YNAB connection) with routing rules (sender rules, currency rules). Splitting these into dedicated pages makes each page's purpose clear at a glance.
Output: New /rules page, updated nav bar, cleaned up /settings page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Existing component interfaces the executor needs. No codebase exploration required. -->

From src/app/(dashboard)/settings/SenderRulesSection.tsx:
```typescript
interface Props {
  connected: boolean;
  initialBudgetId?: string | null;
}
// Usage: <SenderRulesSection connected={connected} initialBudgetId={budgetId ?? null} />
```

From src/app/(dashboard)/settings/CurrencyRulesSection.tsx:
```typescript
interface Props {
  connected: boolean;
  initialBudgetId?: string | null;
}
// Usage: <CurrencyRulesSection connected={connected} initialBudgetId={budgetId ?? null} />
```

From src/app/(dashboard)/settings/page.tsx (current state):
- Imports SettingsForm, SenderRulesSection, CurrencyRulesSection, AdminPasswordSection, ApiKeysSection, YnabConnectionSection
- Reads testMode and budgetId/connected from getSetting()
- budgetId and connected are only needed for SenderRulesSection and CurrencyRulesSection

From src/app/(dashboard)/layout.tsx (current nav):
```tsx
<a href="/dashboard">Dashboard</a>
<a href="/logs">Activity Log</a>
<a href="/settings">Settings</a>
<a href="/tools">Tools</a>
```
Target nav order: Dashboard | Activity Log | Rules | Settings | Tools
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create /rules page and loading skeleton</name>
  <files>src/app/(dashboard)/rules/page.tsx, src/app/(dashboard)/rules/loading.tsx</files>
  <action>
    Create src/app/(dashboard)/rules/page.tsx:
    - Mark with `export const dynamic = 'force-dynamic'`
    - Auth guard: import getAdminSession, redirect to /login if not logged in
    - Read YNAB_BUDGET_ID via getSetting to derive `connected` and `budgetId` (same pattern as settings/page.tsx)
    - Import SenderRulesSection and CurrencyRulesSection from '../settings/SenderRulesSection' and '../settings/CurrencyRulesSection'
    - Render page heading "Rules" with subtitle "Configure sender and currency routing rules."
    - Render <SenderRulesSection connected={connected} initialBudgetId={budgetId ?? null} />
    - Render <CurrencyRulesSection connected={connected} initialBudgetId={budgetId ?? null} />
    - Use identical inline style pattern to settings/page.tsx for h1 and p (fontSize 1.375rem / 0.875rem, colors #111827 / #6b7280)

    Create src/app/(dashboard)/rules/loading.tsx:
    - Export default function RulesLoading
    - Render a simple skeleton that matches the two-section layout: two card-shaped divs with rounded corners, gray background (#f3f4f6), height ~120px each, marginBottom 1.5rem
    - Wrap in same padding context as main layout: <div style={{ padding: '1.5rem' }}>
    - This satisfies the CLAUDE.md perceived-performance requirement for loading states
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Both files exist, TypeScript compiles without errors, rules page imports from ../settings/ correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Add Rules nav link and clean up Settings page</name>
  <files>src/app/(dashboard)/layout.tsx, src/app/(dashboard)/settings/page.tsx</files>
  <action>
    In src/app/(dashboard)/layout.tsx:
    - Add a Rules link between "Activity Log" and "Settings":
      `<a href="/rules" style={{ color: '#374151', textDecoration: 'none' }}>Rules</a>`
    - Final nav order: Dashboard | Activity Log | Rules | Settings | Tools
    - No other changes to layout.tsx

    In src/app/(dashboard)/settings/page.tsx:
    - Remove the import for SenderRulesSection
    - Remove the import for CurrencyRulesSection
    - Remove the `const budgetId = await getSetting('YNAB_BUDGET_ID')` line (no longer needed)
    - Remove the `const connected = !!budgetId` line (no longer needed)
    - Remove the JSX comment "4. Sender Routing Rules" and <SenderRulesSection ... /> element
    - Remove the JSX comment "5. Currency Routing" and <CurrencyRulesSection ... /> element
    - Keep everything else exactly as-is: SettingsForm, ApiKeysSection, YnabConnectionSection, AdminPasswordSection
    - The testMode read stays (still needed for SettingsForm — that moves in plan 03, not here)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - layout.tsx nav has 5 links (Dashboard, Activity Log, Rules, Settings, Tools)
    - settings/page.tsx has no SenderRulesSection or CurrencyRulesSection imports or JSX
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- grep -r "SenderRulesSection\|CurrencyRulesSection" src/app/(dashboard)/settings/page.tsx — zero matches
- grep -n "/rules" src/app/(dashboard)/layout.tsx — shows the Rules anchor
- grep -n "SenderRulesSection\|CurrencyRulesSection" src/app/(dashboard)/rules/page.tsx — shows both imports
- npx tsc --noEmit — zero errors
</verification>

<success_criteria>
- /rules page renders SenderRulesSection and CurrencyRulesSection
- /rules/loading.tsx provides skeleton placeholders
- Nav bar shows Rules link between Activity Log and Settings
- /settings page no longer renders routing rules sections
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/27-settings-restructure-and-label-cleanup/27-02-SUMMARY.md`
</output>
