---
phase: 17-ynab-oauth-token-management
plan: "06"
type: execute
wave: 5
depends_on:
  - "17-05"
files_modified: []
autonomous: false
requirements:
  - YNAB-01
  - YNAB-02
  - YNAB-03
  - YNAB-04
  - YNAB-05

must_haves:
  truths:
    - "User can click 'Connect YNAB', complete the OAuth consent screen, and return to the app connected"
    - "User can select a budget and account from the settings page after connecting"
    - "User can disconnect YNAB and the settings page shows the disconnected state"
    - "Tokens in the database are NOT plaintext JWT strings (they are encrypted ciphertext)"
    - "Clicking 'Connect YNAB' again after disconnecting restores the connection"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: confirm the full YNAB OAuth flow works end-to-end in the live environment, tokens are encrypted at rest, and the settings page correctly reflects connection state.

Purpose: OAuth flows require real browser interaction with the YNAB consent screen — this cannot be automated. This checkpoint also validates token encryption by inspecting the database directly.
Output: Confirmed YNAB-01 through YNAB-05 working in production.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify full Phase 17 YNAB OAuth flow end-to-end in browser</name>
  <action>Human verification — no automated action. See how-to-verify steps below.</action>
  <verify>Human performs steps below and types "approved".</verify>
  <done>All 5 YNAB requirements confirmed passing by the user in the live app.</done>
  <what-built>
    Full YNAB OAuth flow:
    - Connect YNAB button on settings page → /api/ynab/authorize → YNAB consent screen → /api/ynab/callback → encrypted token storage
    - Budget and account selection UI on settings page (dropdowns, persisted selection)
    - Disconnect button clears all token fields
    - AES-256-GCM encrypted tokens in database (not plaintext)
    - Auto-refresh logic in getValidYnabToken (proactive check before API calls, concurrency mutex)
  </what-built>
  <how-to-verify>
    1. **YNAB-01 — OAuth Connect:**
       a. Visit /dashboard/settings (you must be logged in)
       b. Click "Connect YNAB" button
       c. Verify you are redirected to app.ynab.com OAuth consent screen
       d. Grant permission on the consent screen
       e. Verify you are redirected back to /dashboard/settings?ynab_connected=true
       f. Verify the settings page now shows "Connected" status (budget/account selectors visible)

    2. **YNAB-02 — Encrypted tokens:**
       Run via Railway SSH or direct DB query:
       ```sql
       SELECT id, email, "oauthToken" FROM "User" WHERE "oauthToken" IS NOT NULL LIMIT 1;
       ```
       Verify: the oauthToken value starts with hex characters (e.g. `a3f2...`) NOT with `eyJ0eX` (which is a plaintext JWT). The format should be `{nonce_hex}:{ciphertext_hex}:{authTag_hex}`.

    3. **YNAB-05 — Budget and account selection:**
       a. On settings page (after connecting), open the budget dropdown
       b. Select a budget from the list
       c. Verify the account dropdown populates with accounts for that budget
       d. Select an account and save
       e. Refresh the page — verify the previously selected budget and account are still shown

    4. **YNAB-04 — Disconnect:**
       a. Click "Disconnect YNAB"
       b. Verify the settings page returns to "Connect YNAB" disconnected state
       c. Run DB query: `SELECT "oauthToken", "oauthRefreshToken" FROM "User" WHERE email = 'manuelkuhs@gmail.com'`
       d. Verify both columns are NULL

    5. **YNAB-03 — Auto-refresh (code review, cannot wait 2 hours for real expiry):**
       a. Open src/lib/ynab.ts and verify the proactive expiry check uses `5 * 60 * 1000` buffer
       b. Verify the refresh POST to `https://app.ynab.com/oauth/token` is present with `grant_type: 'refresh_token'`
       c. Verify lastRefreshAttemptAt mutex logic: second concurrent call re-reads from DB instead of calling refresh again

    Expected outcomes:
    - Steps 1-4 all pass: YNAB-01, YNAB-02, YNAB-04, YNAB-05 verified in live app
    - Step 5 code review: YNAB-03 implementation verified (live 2-hour test deferred)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human verification complete. All 5 YNAB requirements confirmed working.
</verification>

<success_criteria>
- YNAB OAuth flow completes end-to-end in browser (YNAB-01)
- Database tokens are encrypted ciphertext, not plaintext JWTs (YNAB-02)
- Auto-refresh logic present in code and confirmed by review (YNAB-03)
- Disconnect clears all token fields (YNAB-04)
- Budget and account selection persists across page refresh (YNAB-05)
</success_criteria>

<output>
After completion, create `.planning/phases/17-ynab-oauth-token-management/17-06-SUMMARY.md`
</output>
