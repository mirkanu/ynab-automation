---
phase: 18-per-user-inbound-email
plan: "05"
type: execute
wave: 3
depends_on:
  - "18-03"
  - "18-04"
files_modified: []
autonomous: false

requirements:
  - EMAIL-01
  - EMAIL-02
  - EMAIL-03
  - EMAIL-04
  - EMAIL-05

must_haves:
  truths:
    - "User's forwarding email address is visible somewhere in the UI"
    - "An email forwarded to the user's address creates a YNAB transaction"
    - "Forwarding the same email twice does not create a duplicate YNAB transaction"
    - "A POST to /api/email/inbound from a non-Postmark IP returns 403"
    - "Full test suite is green before human verify"
  artifacts:
    - path: "tests/email/"
      provides: "All 5 email test files green"
  key_links:
    - from: "User.forwardingEmail"
      to: "Postmark inbound domain"
      via: "Address visible in settings or dashboard"
      pattern: "forwardingEmail"
---

<objective>
Human verification checkpoint for Phase 18. Confirms the end-to-end email routing pipeline works in the live Railway environment with real Postmark webhooks.

Purpose: Automated tests validate logic with mocks. This checkpoint confirms the real environment is correctly wired: Postmark domain configured, IP allowlist active, live email creates real YNAB transaction.
Output: Human confirms all 5 success criteria from ROADMAP Phase 18.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-per-user-inbound-email/18-03-SUMMARY.md
@.planning/phases/18-per-user-inbound-email/18-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification automated checks</name>
  <files></files>
  <action>
Run automated validation before surfacing human checkpoint:

1. Full test suite: `npm test` — must be green
2. TypeScript: `npx tsc --noEmit` — must be clean
3. Verify forwarding address assigned to initial user (if DB accessible):
   - Check `User.forwardingEmail` is non-null for existing users
   - Verify `EmailForwardingAddress` table has at least one row
4. Verify env vars present for Postmark (warn if missing, don't block):
   - `POSTMARK_IPS` — Postmark IP allowlist
   - `POSTMARK_INBOUND_DOMAIN` — inbound domain (default: inbound.postmarkapp.com)

Report results of all checks before the human checkpoint begins.
  </action>
  <verify>
    <automated>cd /data/home/ynab && npm test 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>Full test suite green, TypeScript clean, env var status reported</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — Phase 18 end-to-end email pipeline</name>
  <files></files>
  <action>
Pause for human verification of the live Railway environment. All 5 EMAIL requirements must be confirmed before Phase 18 is complete.
  </action>
  <what-built>
Phase 18 Per-User Inbound Email:
- New users get unique Postmark forwarding addresses on signup (EMAIL-01)
- POST /api/email/inbound routes webhooks to correct user via MailboxHash (EMAIL-02)
- Non-Postmark IPs are rejected with 403 (EMAIL-03)
- Duplicate MessageIDs are silently skipped — YNAB not called twice (EMAIL-04)
- Valid forwarded email creates YNAB transaction in user's account (EMAIL-05)
  </what-built>
  <how-to-verify>
**Test 1 — Forwarding address visible (EMAIL-01)**
1. Log in to the app
2. Navigate to Settings
3. Confirm your unique forwarding email address is displayed (format: user_xxxxx_yyyy@inbound.postmarkapp.com)
4. Check DB: `SELECT "forwardingEmail" FROM "User" WHERE email = 'your@email.com'` — must be non-null

**Test 2 — Forged webhook rejected (EMAIL-03)**
```bash
curl -X POST https://your-app.railway.app/api/email/inbound \
  -H "Content-Type: application/json" \
  -d '{"MessageID":"test-123","From":"attacker@example.com","To":"anything@inbound.postmarkapp.com"}'
```
Expected: 403 Forbidden (not from Postmark IP)

**Test 3 — Real email creates YNAB transaction (EMAIL-05)**
1. Forward an Amazon order confirmation email to your forwarding address
2. Wait ~60 seconds for Postmark to deliver the webhook
3. Check YNAB — new transaction should appear with correct amount, payee, and account
4. Check activity log in the app — should show a "success" entry

**Test 4 — Duplicate email skipped (EMAIL-04)**
1. Forward the same email again (or trigger a Postmark webhook retry)
2. Check YNAB — no duplicate transaction created
3. Check activity log — second entry should show "duplicate" or no new entry

**Test 5 — Multi-user isolation (EMAIL-02)**
(If you have a second test account available)
1. Log in as User A, note forwarding address A
2. Log in as User B (different browser/incognito), note forwarding address B
3. Send email to address A
4. Verify transaction appears in User A's YNAB, NOT User B's
  </how-to-verify>
  <verify>
    <automated>MISSING — checkpoint requires human interaction</automated>
  </verify>
  <done>Human confirms all 5 EMAIL requirements pass in the live environment</done>
  <resume-signal>Type "approved" if all 5 tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 18 success criteria (from ROADMAP):
1. Each user has a unique forwarding email address visible in their dashboard
2. An email forwarded to User A's address creates a transaction in User A's YNAB account, not User B's
3. Forwarding the same email twice does not create a duplicate YNAB transaction
4. A forged/unsigned inbound webhook is rejected (returns 4xx, no transaction created)
5. A forwarded order confirmation email results in a YNAB transaction with the correct amount, payee, and account
</verification>

<success_criteria>
Human confirms all 5 EMAIL requirements satisfied in the live Railway environment.
Full test suite green. Phase 18 complete — ready for Phase 19 (Dashboard, Onboarding & Account Management).
</success_criteria>

<output>
After completion, create `.planning/phases/18-per-user-inbound-email/18-05-SUMMARY.md`
</output>
