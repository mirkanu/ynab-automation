---
phase: 08-publishing-readiness
plan: 01
type: mixed
wave: 1
depends_on: ["07-01"]
files_modified:
  - README.md
  - config.example.json    # new
autonomous: false
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DEP-01
  - DEP-02
  - DEP-03
---

<objective>
Make the repo ready for public consumption: rewrite README as a real setup guide, add
config.example.json, migrate the live Railway deployment to the new env vars
(pre-configured for Manuel and Emily-Kate), and make the GitHub repo public.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<new_env_vars>
After Phase 7, the app expects:
  SENDERS          - JSON array of sender configs
  CURRENCY_ACCOUNTS - JSON object of currency overrides (optional)
  ADMIN_EMAIL      - who receives error notifications
  (plus unchanged: DATABASE_URL, ANTHROPIC_API_KEY, YNAB_PERSONAL_ACCESS_TOKEN, YNAB_BUDGET_ID, RESEND_API_KEY)

Replaces:
  YNAB_MANUEL_ACCOUNT_ID, YNAB_EMILY_ACCOUNT_ID, EMILY_KATE_EMAIL,
  YNAB_EURO_ACCOUNT_ID, MANUEL_EMAIL
</new_env_vars>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config.example.json</name>
  <files>config.example.json</files>
  <action>
Create config.example.json showing the full configuration structure with placeholder values:

```json
{
  "_comment": "This file documents the config structure. Set SENDERS and CURRENCY_ACCOUNTS as env vars using the formats shown below.",
  "SENDERS_example": [
    {
      "email": "alice@example.com",
      "name": "Alice",
      "accountId": "ynab-account-uuid-alice",
      "notificationLabel": "Alice"
    },
    {
      "email": "bob@example.com",
      "name": "Bob",
      "accountId": "ynab-account-uuid-bob"
    }
  ],
  "CURRENCY_ACCOUNTS_example": {
    "EUR": "ynab-euro-account-uuid"
  },
  "_notes": [
    "notificationLabel is optional — if set, it appears in error notification subjects e.g. 'YNAB automation: failed to parse order email (Alice)'",
    "CURRENCY_ACCOUNTS is optional — omit if all your transactions are in one currency",
    "When setting env vars, use the value of SENDERS_example as the SENDERS value (a raw JSON array string)",
    "Remove the _comment, _notes, and _example suffixes — those are documentation only"
  ]
}
```
  </action>
  <done>config.example.json created with placeholder values and documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md as a setup guide for third parties</name>
  <files>README.md</files>
  <action>
Rewrite README.md. It should be a complete, self-contained guide for a new user deploying their own instance. Structure:

1. **What it does** — 2-3 sentences, no personal references
2. **How it works** — brief pipeline description (Forward email → Pipedream → Railway → Claude → YNAB)
3. **Features** — bullet list (any retailer, multi-user, currency routing, category tagging, dedup, error notifications)
4. **Prerequisites** — accounts needed: Railway, YNAB (personal access token), Anthropic API key, Pipedream, Resend
5. **Setup guide** — step-by-step:
   a. Fork and clone the repo
   b. Deploy to Railway (link to Railway docs), provision PostgreSQL
   c. Configure Pipedream inbound email (point to Railway webhook URL)
   d. Set Railway env vars — table of all vars with descriptions, noting which are required vs optional
   e. Set the SENDERS env var — show format, reference config.example.json
   f. Test: forward an order confirmation email, check Railway logs, verify YNAB transaction
6. **Optional: category tagging** — explain the first-line hint feature
7. **Optional: currency accounts** — explain CURRENCY_ACCOUNTS for multi-currency households
8. **Stack** — tech table (same as before)
9. **Development** — npm commands
10. **Project structure** — src/ tree
11. **Built with** — Claude Code + GSD attribution (keep this section, it's important to the user)

The README must contain zero personal references (no Manuel, Emily, manuelkuhs, etc.).
  </action>
  <done>README.md rewritten as a generic setup guide. No personal references.</done>
</task>

<task type="human" checkpoint="true">
  <name>Task 3: Migrate Railway env vars to new config format</name>
  <action>
The live Railway deployment needs to be updated to use the new env var names.

**Step 1 — read current values from Railway:**
Run: `railway variables` and note the current values of:
- YNAB_MANUEL_ACCOUNT_ID  (→ will become part of SENDERS)
- YNAB_EMILY_ACCOUNT_ID   (→ will become part of SENDERS)
- EMILY_KATE_EMAIL         (→ will become part of SENDERS)
- YNAB_EURO_ACCOUNT_ID     (→ will become CURRENCY_ACCOUNTS)
- MANUEL_EMAIL             (→ will become ADMIN_EMAIL)

**Step 2 — build the new env var values:**

SENDERS (replace UUIDs with actual values from Step 1):
```json
[{"email":"manuelkuhs@gmail.com","name":"Manuel","accountId":"<YNAB_MANUEL_ACCOUNT_ID>"},{"email":"<EMILY_KATE_EMAIL>","name":"Emily-Kate","accountId":"<YNAB_EMILY_ACCOUNT_ID>","notificationLabel":"Emily-Kate"}]
```

CURRENCY_ACCOUNTS (replace UUID with actual value from Step 1):
```json
{"EUR":"<YNAB_EURO_ACCOUNT_ID>"}
```

ADMIN_EMAIL: the current value of MANUEL_EMAIL

**Step 3 — set new variables on Railway:**
```bash
railway variables set SENDERS='[...]'
railway variables set CURRENCY_ACCOUNTS='{"EUR":"..."}'
railway variables set ADMIN_EMAIL='...'
```

**Step 4 — remove old variables:**
```bash
railway variables delete YNAB_MANUEL_ACCOUNT_ID
railway variables delete YNAB_EMILY_ACCOUNT_ID
railway variables delete EMILY_KATE_EMAIL
railway variables delete YNAB_EURO_ACCOUNT_ID
railway variables delete MANUEL_EMAIL
```

**Step 5 — verify deployment:**
Check Railway logs after redeploy — app should start without config errors.
Send a test forward, confirm transaction created correctly.
  </action>
  <done>Railway updated. New env vars set. Old vars removed. App starts cleanly. Test transaction confirmed.</done>
</task>

<task type="human">
  <name>Task 4: Make GitHub repo public</name>
  <action>
Go to https://github.com/mirkanu/ynab-automation/settings and change visibility from Private to Public.
Or run: `gh repo edit mirkanu/ynab-automation --visibility public`
  </action>
  <done>Repo is public at https://github.com/mirkanu/ynab-automation</done>
</task>

</tasks>

<verification>
- config.example.json exists and documents the full structure
- README has no personal references and serves as a complete setup guide
- Railway deployment starts cleanly with new env vars
- Test transaction created successfully after Railway migration
- https://github.com/mirkanu/ynab-automation is publicly accessible
</verification>

<success_criteria>
- config.example.json with placeholder values and format documentation
- README rewritten as generic setup guide, zero personal references
- Live Railway deployment running on new env var names
- Old personal env vars removed from Railway
- GitHub repo is public
</success_criteria>

<output>
After completion, create `.planning/phases/08-publishing-readiness/08-01-SUMMARY.md`
</output>
