---
phase: 19-dashboard-onboarding-account-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260402000000_user_testmode_onboarding/migration.sql
  - tests/dashboard/activity-log.test.ts
  - tests/dashboard/stats.test.ts
  - tests/dashboard/test-mode.test.ts
  - tests/dashboard/parse-transparency.test.ts
  - tests/onboarding/flow.test.ts
  - tests/onboarding/email-providers.test.ts
  - tests/account/deletion.test.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - ONBD-01
  - ONBD-02
  - ONBD-03
  - ONBD-04

must_haves:
  truths:
    - "User model has testMode boolean field (default false)"
    - "User model has onboardingCompleted boolean field (default false)"
    - "All Wave 0 test stubs exist and test suite is green (todos, not failures)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "testMode and onboardingCompleted fields on User model"
      contains: "testMode"
    - path: "prisma/migrations/20260402000000_user_testmode_onboarding/migration.sql"
      provides: "ALTER TABLE migration adding the two columns"
    - path: "tests/dashboard/activity-log.test.ts"
      provides: "Wave 0 stubs for DASH-01"
    - path: "tests/dashboard/stats.test.ts"
      provides: "Wave 0 stubs for DASH-02"
    - path: "tests/dashboard/test-mode.test.ts"
      provides: "Wave 0 stubs for DASH-04"
    - path: "tests/dashboard/parse-transparency.test.ts"
      provides: "Wave 0 stubs for DASH-05"
    - path: "tests/onboarding/flow.test.ts"
      provides: "Wave 0 stubs for ONBD-01"
    - path: "tests/onboarding/email-providers.test.ts"
      provides: "Wave 0 stubs for ONBD-02"
    - path: "tests/account/deletion.test.ts"
      provides: "Wave 0 stubs for ONBD-03"
  key_links:
    - from: "prisma/schema.prisma"
      to: "prisma/migrations/20260402000000_user_testmode_onboarding/migration.sql"
      via: "manual migration SQL matching schema changes"
      pattern: "ALTER TABLE.*User.*ADD COLUMN.*testMode"
---

<objective>
Add two new fields to the User model (testMode, onboardingCompleted) via Prisma schema + manual migration SQL, then create all Wave 0 test stubs so the suite stays green for downstream plans.

Purpose: Plans 02-04 depend on testMode being in the DB and test files existing. Schema migration must come first. Wave 0 stubs keep Vitest green while source modules don't exist yet.
Output: Updated schema, migration SQL file, 7 test stub files with it.todo() stubs only.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-dashboard-onboarding-account-management/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add testMode and onboardingCompleted to User schema + migration SQL</name>
  <files>prisma/schema.prisma, prisma/migrations/20260402000000_user_testmode_onboarding/migration.sql</files>
  <action>
    In prisma/schema.prisma, add two new fields to the User model immediately after the `forwardingEmail` line:

    ```
    // Phase 19: per-user test mode and onboarding state
    testMode             Boolean   @default(false)
    onboardingCompleted  Boolean   @default(false)
    ```

    Then create the migration directory and SQL file manually (no local PostgreSQL, cannot run prisma migrate dev):

    Create prisma/migrations/20260402000000_user_testmode_onboarding/migration.sql:
    ```sql
    -- Phase 19: Add testMode and onboardingCompleted to User table
    ALTER TABLE "User" ADD COLUMN "testMode" BOOLEAN NOT NULL DEFAULT false;
    ALTER TABLE "User" ADD COLUMN "onboardingCompleted" BOOLEAN NOT NULL DEFAULT false;
    ```

    IMPORTANT: Do NOT run `prisma migrate dev` — Railway-only PostgreSQL. Verify schema compiles with `npx prisma validate`.

    This migration follows the pattern established in Phase 16 (manually authored SQL) and Phase 18 (manually authored SQL for EmailForwardingAddress/ProcessedWebhook).
  </action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>prisma validate exits 0; schema.prisma contains testMode and onboardingCompleted on User model; migration SQL file exists at correct path</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test stubs for all Phase 19 requirements</name>
  <files>
    tests/dashboard/activity-log.test.ts,
    tests/dashboard/stats.test.ts,
    tests/dashboard/test-mode.test.ts,
    tests/dashboard/parse-transparency.test.ts,
    tests/onboarding/flow.test.ts,
    tests/onboarding/email-providers.test.ts,
    tests/account/deletion.test.ts
  </files>
  <action>
    Create 7 test stub files using it.todo() stubs only (no imports from source modules — avoids test runner errors when source doesn't exist yet). Follow the exact same pattern from Phase 17 (tests/ynab/*.test.ts) and Phase 18 (tests/email/*.test.ts).

    tests/dashboard/activity-log.test.ts:
    ```ts
    import { describe, it } from 'vitest'
    describe('DASH-01: Activity log per-user filtering', () => {
      it.todo('getActivityLogs filters by userId — no cross-user entries returned')
      it.todo('getActivityLogs respects status filter')
      it.todo('getActivityLogs respects date range filter')
      it.todo('getActivityLogs returns empty array for user with no logs')
    })
    ```

    tests/dashboard/stats.test.ts:
    ```ts
    import { describe, it } from 'vitest'
    describe('DASH-02: Dashboard stats per-user scoping', () => {
      it.todo('getDashboardStats returns only current user stats')
      it.todo('getDashboardStats this-week window is calculated correctly')
      it.todo('getDashboardStats success rate is 0 when no logs exist')
      it.todo('getDashboardStats lastTransaction is null when no logs exist')
    })
    ```

    tests/dashboard/test-mode.test.ts:
    ```ts
    import { describe, it } from 'vitest'
    describe('DASH-04: Per-user test mode toggle', () => {
      it.todo('toggleTestMode sets testMode=true on User row')
      it.todo('toggleTestMode sets testMode=false on User row')
      it.todo('testMode is isolated per user — one user toggling does not affect another')
      it.todo('testMode persists across requests (read from DB each time)')
    })
    ```

    tests/dashboard/parse-transparency.test.ts:
    ```ts
    import { describe, it } from 'vitest'
    describe('DASH-05: Parse transparency display', () => {
      it.todo('formatParseResult extracts retailer, amount, date, currency from parseResult JSON')
      it.todo('formatParseResult returns null fields when parseResult is null')
      it.todo('formatParseResult does not expose raw token counts or stop_reason debug fields')
      it.todo('formatParseResult handles missing optional fields gracefully')
    })
    ```

    tests/onboarding/flow.test.ts:
    ```ts
    import { describe, it } from 'vitest'
    describe('ONBD-01: Onboarding flow completion', () => {
      it.todo('new user has onboardingCompleted=false after account creation')
      it.todo('markOnboardingComplete sets onboardingCompleted=true on User row')
      it.todo('user with onboardingCompleted=true is not redirected to /onboarding')
    })
    ```

    tests/onboarding/email-providers.test.ts:
    ```ts
    import { describe, it } from 'vitest'
    describe('ONBD-02: Email provider detection and instructions', () => {
      it.todo('detectEmailProvider returns gmail for gmail.com and googlemail.com')
      it.todo('detectEmailProvider returns outlook for outlook.com, hotmail.com, live.com')
      it.todo('detectEmailProvider returns apple for icloud.com and me.com')
      it.todo('detectEmailProvider returns other for unknown domains')
      it.todo('getForwardingInstructions returns different steps for each provider')
      it.todo('getForwardingInstructions replaces [address] placeholder with actual forwarding address')
    })
    ```

    tests/account/deletion.test.ts:
    ```ts
    import { describe, it } from 'vitest'
    describe('ONBD-03: Account deletion and data cascade', () => {
      it.todo('DELETE /api/account/delete returns 401 for unauthenticated request')
      it.todo('DELETE /api/account/delete removes User row')
      it.todo('after deletion, ActivityLog rows for that userId are removed (CASCADE)')
      it.todo('after deletion, Setting rows for that userId are removed (CASCADE)')
      it.todo('after deletion, EmailForwardingAddress rows for that userId are removed (CASCADE)')
    })
    ```

    After creating all files, run vitest to confirm the suite is green (todos render as skipped, not failures).
  </action>
  <verify>
    <automated>npx vitest run tests/dashboard tests/onboarding tests/account --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 7 test files exist; npx vitest run exits 0; all stubs show as "todo" (skipped), no failures; TypeScript has no compilation errors on the test files</done>
</task>

</tasks>

<verification>
npx prisma validate
npx vitest run tests/dashboard tests/onboarding tests/account --reporter=verbose
</verification>

<success_criteria>
- prisma/schema.prisma contains `testMode Boolean @default(false)` and `onboardingCompleted Boolean @default(false)` on User model
- Migration SQL file exists at prisma/migrations/20260402000000_user_testmode_onboarding/migration.sql with two ALTER TABLE statements
- All 7 Wave 0 test stub files exist
- npx vitest run exits 0 (todos counted as skipped, not failed)
</success_criteria>

<output>
After completion, create `.planning/phases/19-dashboard-onboarding-account-management/19-01-SUMMARY.md`
</output>
