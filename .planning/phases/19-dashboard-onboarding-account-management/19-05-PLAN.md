---
phase: 19-dashboard-onboarding-account-management
plan: 05
type: execute
wave: 3
depends_on:
  - 19-02
  - 19-03
  - 19-04
files_modified: []
autonomous: false

requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - ONBD-01
  - ONBD-02
  - ONBD-03
  - ONBD-04

must_haves:
  truths:
    - "Full test suite passes (all phases)"
    - "Activity log shows per-user data; no cross-user leakage"
    - "Dashboard stats show this-week totals for the current user"
    - "Test mode toggles per-user and banner reflects DB state"
    - "Parse transparency section visible in expanded log rows"
    - "New user signup flow redirects to /onboarding with guided steps"
    - "Email provider instructions match the user's email domain"
    - "Settings page has account deletion section with confirmation"
    - "Public homepage accessible without login; has Sign up CTA"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Per-user dashboard with stats and forwarding email"
    - path: "src/app/(dashboard)/settings/page.tsx"
      provides: "Settings with YNAB connection, test mode, account deletion"
    - path: "src/app/onboarding/page.tsx"
      provides: "3-step onboarding flow for new users"
    - path: "src/app/page.tsx"
      provides: "Public homepage with signup CTA"
    - path: "src/app/api/account/delete/route.ts"
      provides: "GDPR account deletion with cascade"
  key_links:
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "/onboarding"
      via: "redirect when onboardingCompleted=false"
      pattern: "redirect.*onboarding"
    - from: "src/app/page.tsx"
      to: "/auth/signin"
      via: "Sign up CTA link"
      pattern: "auth/signin"
---

<objective>
Human verification checkpoint for all Phase 19 features. Confirm the complete user journey works end-to-end in the live application.

Purpose: Automated tests verify logic; human verification confirms the actual user experience works in a real browser with real sessions.
Output: Verified UX for all 9 Phase 19 requirements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated test suite + TypeScript verification</name>
  <files></files>
  <action>
    Run the complete test suite and TypeScript compilation. Fix any remaining failures before presenting checkpoint.

    Commands:
    1. npx vitest run --reporter=verbose
    2. npx tsc --noEmit

    If any tests fail or TS errors exist, fix them before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20 && npx tsc --noEmit</automated>
  </verify>
  <done>All tests pass; TypeScript compiles with no errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 19 complete feature set:
    - DASH-01/02: Per-user activity log and dashboard stats (shows only current user's data)
    - DASH-03: Settings page with per-user YNAB connection, test mode toggle, forwarding email
    - DASH-04: Per-user test mode toggle (DB-backed, isolated per user)
    - DASH-05: Parse transparency in activity log rows (collapsible "Claude's Reasoning")
    - ONBD-01: Guided onboarding flow for new users (3 steps: YNAB → address → instructions)
    - ONBD-02: Provider-specific email forwarding instructions (Gmail, Outlook, Apple, other)
    - ONBD-03: Account deletion with GDPR cascade from Settings page
    - ONBD-04: Public homepage at / with product explanation and signup CTA
  </what-built>
  <how-to-verify>
    Run the app locally or check Railway deployment. Verify each area:

    **ONBD-04 — Public Homepage:**
    1. Visit / while logged out (or open in incognito)
    2. Confirm: Page loads without redirect to /auth/signin
    3. Confirm: Product description is visible ("Your order emails, automatically in YNAB")
    4. Confirm: "Sign up free" CTA button is visible and links to /auth/signin
    5. Confirm: "How it works" section shows 4 steps

    **ONBD-01 — New User Onboarding:**
    6. Sign in as a new user (use an email you haven't signed up with before)
    7. Confirm: After magic link click, you land on /onboarding (not /)
    8. Confirm: 3 steps visible — YNAB connection, forwarding address, email setup instructions
    9. Confirm: "Connect YNAB" step shows a button linking to YNAB OAuth
    10. After completing onboarding, click "Go to dashboard"
    11. Confirm: Next login goes directly to /dashboard (onboardingCompleted=true)

    **ONBD-02 — Email Provider Instructions:**
    12. Check the onboarding page Step 3 for your email provider (Gmail shows Gmail steps, Outlook shows Outlook steps)
    13. Confirm: Instructions include your actual forwarding email address in the steps

    **DASH-01/02 — Dashboard and Activity Log:**
    14. Visit /dashboard as an authenticated user
    15. Confirm: Stats show "This Week" and "Success Rate" cards
    16. Confirm: Forwarding address card shows YOUR unique address (from DB, not env var)
    17. Visit /logs
    18. Confirm: Activity log shows only your entries, with correct sender/subject

    **DASH-05 — Parse Transparency:**
    19. Click any log entry that has a successful parse result
    20. Confirm: Expanded view shows "Claude's Reasoning" section with retailer, amount, date
    21. Confirm: No debug fields visible (stop_reason, token counts, etc.)

    **DASH-03/04 — Settings and Test Mode:**
    22. Visit /settings
    23. Confirm: Test mode toggle is visible and reflects current state
    24. Toggle test mode ON → confirm banner appears at top of all dashboard pages
    25. Toggle test mode OFF → confirm banner disappears
    26. Confirm: Your forwarding email is displayed in Settings (read-only)

    **ONBD-03 — Account Deletion:**
    27. Visit /settings → scroll to "Danger Zone" section
    28. Confirm: "Delete Account" button is visible with a warning about permanence
    29. Click Delete Account → confirm a confirmation step appears
    30. DO NOT complete deletion (this is a live account check only)
    31. Confirm: Cancelling the deletion step returns to Settings without any changes
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
npx tsc --noEmit
</verification>

<success_criteria>
- All automated tests pass (full suite)
- TypeScript compiles clean
- Human verification: all 9 requirement areas confirmed working in live app
- v5.0 milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/19-dashboard-onboarding-account-management/19-05-SUMMARY.md`
</output>
