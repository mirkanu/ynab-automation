---
phase: 24-test-suite-cleanup-and-self-host-docs
plan: 04
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-02
  - 24-03
files_modified: []
autonomous: false
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - CLEAN-04

must_haves:
  truths:
    - "npm test passes with zero failing and zero skipped tests (CI green)"
    - "Deploy on Railway button produces a Railway template preview with PostgreSQL and IRON_SESSION_SECRET"
    - "A fresh Railway deployment completes the wizard and receives a forwarded email successfully"
    - "The README walkthrough is complete without consulting any file outside the README"
    - "v6.0 milestone is verified end-to-end"
  artifacts:
    - path: "README.md"
      provides: "Verified self-host documentation"
    - path: "docs/images/wizard-step-1.png"
      provides: "Wizard screenshot committed and rendering in README"
  key_links:
    - from: "README.md"
      to: "railway.com/template"
      via: "Deploy button click"
      pattern: "Deploy on Railway"
---

<objective>
Human verification checkpoint: walk the complete README from "clicked GitHub link" to "forwarding emails into YNAB" on a fresh Railway deployment. This is the final verification gate for v6.0.

Purpose: Confirm the README is accurate, the Railway deploy button works for a real non-programmer path, and the full self-host flow from deploy to first transaction works end-to-end. This is NOT the existing `ynab-test-production` deployment — it must be a fresh deployment to exercise the first-install wizard as a new user would.

Output: v6.0 verified. Proceed to `/gsd:complete-milestone`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-test-suite-cleanup-and-self-host-docs/24-CONTEXT.md
@.planning/phases/24-test-suite-cleanup-and-self-host-docs/24-01-SUMMARY.md
@.planning/phases/24-test-suite-cleanup-and-self-host-docs/24-02-SUMMARY.md
@.planning/phases/24-test-suite-cleanup-and-self-host-docs/24-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-checks before human verification</name>
  <files></files>
  <action>
Run all automated checks so the human checkpoint focuses on the live app, not mechanical errors.

1. **npm test (full suite):**
   ```bash
   npm test
   ```
   Expected: 0 failing, 0 skipped. If any failures, fix before proceeding.

2. **TypeScript check:**
   ```bash
   npx tsc --noEmit
   ```
   Expected: exits 0. Fix any type errors.

3. **Verify test cleanup is complete:**
   ```bash
   ls tests/ 2>&1  # should say "No such file or directory"
   grep -rn "process\.env\|loadDbSettings" src/lib/settings.ts  # should return nothing
   ```

4. **Verify INBOUND_EMAIL rename:**
   ```bash
   grep -rn "PIPEDREAM_WEBHOOK_URL" src/  # should return nothing
   grep "INBOUND_EMAIL" src/lib/wizard.ts  # should show step 6
   ```

5. **Verify screenshots committed:**
   ```bash
   ls docs/images/wizard-*.png | wc -l  # should be 7
   ```

6. **Verify README structure:**
   ```bash
   wc -l README.md  # should be 400-600
   grep -c "docs/images/wizard-step" README.md  # should be >= 6
   grep "railway.com.*template" README.md  # should match deploy button
   grep -E "anthropic.com/pricing|railway.app/pricing|resend.com/pricing" README.md  # should match 3
   ```

7. **Push to Railway and confirm CI build:**
   ```bash
   git push
   ```
   Wait for Railway auto-deploy to complete on the existing `ynab-test-production` deployment. Confirm green build in Railway dashboard.

If any check fails, fix it before presenting the human checkpoint.
  </action>
  <verify>
    <automated>cd /data/home/ynab && npm test 2>&1 | tail -5 && echo "---" && ls tests/ 2>&1 && echo "---" && wc -l README.md</automated>
  </verify>
  <done>All automated checks pass. git push succeeded. Railway build is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — fresh Railway deployment dry-run</name>
  <files></files>
  <action>
**This verification requires a FRESH Railway deployment — not ynab-test-production. You are verifying as a non-programmer seeing the app for the first time.**

This is the final gate for v6.0. Walk through 5 checks using the README as your only guide.

Resume signal: Type "approved — v6.0 complete" if all checks pass, or describe issues found.
  </action>
  <verify>
### Check 1: Railway Deploy Button (DOCS-01)

1. Open README.md (on GitHub or locally in a markdown preview)
2. Click the "Deploy on Railway" button
3. Confirm the Railway template preview screen shows:
   - PostgreSQL database included in the template
   - `IRON_SESSION_SECRET` listed as an auto-generated variable
   - `DATABASE_URL` shown as connected to PostgreSQL
4. **DO NOT deploy yet** — verify the preview is correct, then close

---

### Check 2: Fresh Deployment Dry-Run (DOCS-02)

Create a fresh Railway deployment using the button:

1. Click Deploy on Railway → complete Railway setup → confirm deploy
2. Wait for deployment health check to go green (`npm run db:migrate && npm start`)
3. Open the fresh deployment URL in an incognito window
4. Confirm redirect to `/setup/1` (wizard, not dashboard or login)

Walk through the wizard **using only the README as your guide**:
5. Step 1 — Admin password. Is the README clear? Screenshot accurate?
6. Step 2 — YNAB PAT. Does the `app.ynab.com/settings/developer` link work? Screenshot accurate?
7. Step 3 — Budget & Account. Does the budget dropdown populate with a valid PAT?
8. Steps 4, 5, 6 — API keys + Pipedream email. Do provider links go to the right pages?
9. Finish Setup → `/setup/done` renders with forwarding email shown
10. Visit `/` → redirects to `/login`
11. Log in with admin password → `/dashboard` loads

---

### Check 3: First Email Forward (DOCS-02)

12. Forward any order confirmation to the Pipedream inbound address from step 6
13. Wait up to 60 seconds
14. Activity Log shows a green "YNAB transaction created" row
15. YNAB shows the transaction in the correct account

---

### Check 4: Costs Section Spot-Check (DOCS-03)

16. Read the "Costs" section in README
17. All 3 providers (Railway, Claude, Resend) have pricing links and plain-language expectations
18. No hard dollar amounts (e.g. "$0.02" or "5 dollars")

---

### Check 5: Troubleshooting Sanity

19. Skim the Troubleshooting section — covers the most likely failure modes?
20. Tone is calm and non-technical?

---

**If any check fails:** describe what failed. Claude will fix and re-deploy before re-verification.
**If all checks pass:** type "approved — v6.0 complete"
  </verify>
  <done>User has typed "approved — v6.0 complete". All 4 Phase 24 success criteria are verified:
1. npm test: 0 failing, 0 skipped
2. Deploy button → Railway template preview with PostgreSQL + IRON_SESSION_SECRET
3. Fresh deploy → wizard → first email → YNAB transaction
4. Costs section: 3 providers, pricing links, no hard dollar amounts</done>
</task>

</tasks>

<verification>
All 4 Phase 24 success criteria are verified by the human checkpoint:
1. npm test: 0 failing, 0 skipped (Task 1 automated check)
2. Deploy button → Railway template preview shows PostgreSQL + IRON_SESSION_SECRET (Check 1)
3. Fresh deploy → wizard → first email → YNAB transaction (Checks 2 + 3)
4. Costs section: 3 providers, pricing links, no hard dollar amounts (Check 4)
</verification>

<success_criteria>
All 4 Phase 24 success criteria from ROADMAP.md are satisfied:
1. `npm test` reports zero failing and zero skipped tests — tests/ deleted, src/*.test.ts all pass
2. "Deploy on Railway" button produces Railway template with PostgreSQL + auto-generated IRON_SESSION_SECRET
3. README walks end-to-end (deploy → wizard → first email) with numbered steps and wizard screenshots, dry-run verified by user
4. README costs section names Claude, Resend, Railway with pricing links and no hard dollar amounts

This is the final phase of v6.0. Verification success means v6.0 is complete. Next: `/gsd:complete-milestone v6.0`
</success_criteria>

<output>
After completion, create `.planning/phases/24-test-suite-cleanup-and-self-host-docs/24-04-SUMMARY.md`

On approval, write `.planning/phases/24-test-suite-cleanup-and-self-host-docs/24-VERIFICATION.md` documenting:
- All 5 human verification checks and their results
- The fresh Railway deployment URL used
- Any issues found and fixed during verification
- Final confirmation: v6.0 complete, ready for /gsd:complete-milestone

Then instruct the user: "Phase 24 complete. v6.0 milestone verified. Run `/gsd:complete-milestone` to archive the milestone and tag the release."
</output>
