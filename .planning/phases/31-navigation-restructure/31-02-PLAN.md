---
phase: 31-navigation-restructure
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - src/app/(dashboard)/layout.tsx
autonomous: false
requirements:
  - NAV-03
  - NAV-04

must_haves:
  truths:
    - "Dashboard page loads with the new two-tier nav showing Dashboard, Email Automation (collapsible), Currency (collapsible), Settings"
    - "Old flat nav items 'Activity Log', 'Rules', 'Tools' no longer appear in the nav bar"
    - "Clicking Email Automation expands the section revealing Activity Log, Rules, Test & Replay sub-items"
    - "Clicking Currency expands the section revealing EUR→GBP Transfers, EUR Conversion, EUR Reconciliation sub-items"
    - "Navigating to each sub-page loads the correct content"
    - "Active page sub-item is bold with color #111827"
  artifacts:
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Dashboard layout importing Navigation component"
      contains: "import Navigation from"
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/app/(dashboard)/components/Navigation.tsx"
      via: "import Navigation"
      pattern: "import Navigation from"
---

<objective>
Wire the Navigation component into the dashboard layout, replacing the flat nav.

Purpose: Swap the static `<nav>` block in layout.tsx for the `<Navigation />` client component created in Plan 01.
Output: Updated layout.tsx — the only file changed in this plan.
</objective>

<execution_context>
@/home/services/ynab/.planning/phases/31-navigation-restructure/../../../get-shit-done/workflows/execute-plan.md
@/home/services/ynab/.planning/phases/31-navigation-restructure/../../../get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/services/ynab/.planning/ROADMAP.md
@/home/services/ynab/.planning/phases/31-navigation-restructure/31-UI-SPEC.md
@/home/services/ynab/.planning/phases/31-navigation-restructure/31-01-SUMMARY.md

<interfaces>
<!-- Current layout.tsx full file — executor must read before editing: -->
<!-- src/app/(dashboard)/layout.tsx -->

<!-- The Navigation component created in Plan 01: -->
<!-- src/app/(dashboard)/components/Navigation.tsx -->
<!-- Exported as: export default function Navigation() -->
<!-- Usage: <Navigation /> (no props required; self-contained) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace flat nav with Navigation component in layout.tsx</name>
  <files>src/app/(dashboard)/layout.tsx</files>
  <read_first>
    - src/app/(dashboard)/layout.tsx — full file; must read current state before editing
    - src/app/(dashboard)/components/Navigation.tsx — verify Navigation export signature before importing
  </read_first>
  <action>
Edit `src/app/(dashboard)/layout.tsx` to make two changes:

**Change 1: Add import for Navigation component**

Add this import after the existing `import TestModeBanner from './components/TestModeBanner'` line:

```tsx
import Navigation from './components/Navigation'
```

**Change 2: Replace the `<nav>` block with `<Navigation />`**

Find the entire `<nav>` block (lines 51-65 in the current file):
```tsx
<nav style={{
  display: 'flex',
  gap: '1.5rem',
  padding: '0.5rem 1.5rem',
  backgroundColor: '#f9fafb',
  borderBottom: '1px solid #e5e7eb',
  fontSize: '0.8125rem',
  fontWeight: 500,
}}>
  <a href="/dashboard" style={{ color: '#374151', textDecoration: 'none' }}>Dashboard</a>
  <a href="/logs" style={{ color: '#374151', textDecoration: 'none' }}>Activity Log</a>
  <a href="/rules" style={{ color: '#374151', textDecoration: 'none' }}>Rules</a>
  <a href="/settings" style={{ color: '#374151', textDecoration: 'none' }}>Settings</a>
  <a href="/tools" style={{ color: '#374151', textDecoration: 'none' }}>Tools</a>
</nav>
```

Replace it entirely with:
```tsx
<Navigation />
```

The layout must remain a Server Component — do NOT add `'use client'` to layout.tsx. The Navigation component handles its own `'use client'` directive. The layout continues to call `getAdminSession()` and `getSetting()` as async server functions.

No other changes to layout.tsx.
  </action>
  <verify>
    <automated>cd /home/services/ynab && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import Navigation from" src/app/\(dashboard\)/layout.tsx` returns 1 match
    - `grep -v "^//" src/app/\(dashboard\)/layout.tsx | grep "href=\"/logs\""` returns 0 matches (old Activity Log link gone)
    - `grep -v "^//" src/app/\(dashboard\)/layout.tsx | grep "href=\"/rules\""` returns 0 matches (old Rules link gone)
    - `grep -v "^//" src/app/\(dashboard\)/layout.tsx | grep "href=\"/tools\""` returns 0 matches (old Tools link gone)
    - `grep "<Navigation" src/app/\(dashboard\)/layout.tsx` returns 1 match
    - File does NOT contain `'use client'` (layout stays as Server Component)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>layout.tsx imports and renders Navigation; old flat nav links removed; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Navigation restructure complete:
    - New Navigation.tsx component with collapsible Email Automation and Currency sections
    - 6 new route pages: /email-automation/logs, /email-automation/rules, /email-automation/tools, /currency/transfers, /currency/reconciliation, /currency/conversion
    - layout.tsx updated to use Navigation component
    - Old flat nav (Activity Log, Rules, Tools top-level items) removed
  </what-built>
  <how-to-verify>
    Deploy the app first (see CLAUDE.md deploy command), then verify:

    1. Open the admin dashboard. Confirm the nav bar shows: Dashboard | Email Automation ▼ | Currency ▼ | Settings
       (no "Activity Log", "Rules", or "Tools" top-level items)

    2. Click "Email Automation" — confirm it expands to show 3 sub-items:
       - Activity Log → clicking loads the activity log table at /email-automation/logs
       - Rules → clicking loads sender + currency rules at /email-automation/rules
       - Test & Replay → clicking loads the test mode toggle + TestParseForm at /email-automation/tools

    3. Click "Email Automation" again — confirm it collapses (sub-items disappear)

    4. Click "Currency" — confirm it expands to show 3 sub-items:
       - EUR→GBP Transfers → clicking loads the FixEurGbpTransfersCard at /currency/transfers
       - EUR Conversion → clicking loads "Coming soon" placeholder at /currency/conversion
       - EUR Reconciliation → clicking loads the ReconcileEurWiseCard at /currency/reconciliation

    5. Navigate to /email-automation/logs — confirm "Activity Log" sub-item in nav appears bold (#111827) as the active page

    6. Navigate to /currency/transfers — confirm "EUR→GBP Transfers" sub-item appears bold as the active page

    7. Verify Dashboard (/dashboard) and Settings (/settings) links still work

    8. Old routes backward compat (optional check): visit /logs, /rules, /tools — confirm they still load (no 404) since they were not deleted
  </how-to-verify>
  <resume-signal>Type "approved" to confirm all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → layout.tsx | Auth guard in layout (getAdminSession) protects all child routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Information Disclosure | layout.tsx Server Component | accept | Layout remains a Server Component — `getAdminSession()` and `getSetting()` run server-side only; no auth logic change; existing protection unchanged |
| T-31-05 | Tampering | layout.tsx `<Navigation />` swap | accept | Navigation is client-only state (expanded/collapsed); no server data flows through Navigation props; the swap is cosmetic only |
</threat_model>

<verification>
1. `npm run build` exits 0
2. `grep "import Navigation from" src/app/(dashboard)/layout.tsx` matches
3. Nav bar shows Dashboard, Email Automation, Currency, Settings (manual check)
4. Sub-items load correct pages (manual check)
5. Active page indicator highlights correct item (manual check)
</verification>

<success_criteria>
- Build passes with no errors
- Human verifies two-tier nav works: Email Automation and Currency sections expand/collapse, sub-pages load correct content
- Old top-level Activity Log, Rules, and Tools nav items no longer visible
- Dashboard and Settings remain accessible as before
</success_criteria>

<output>
After completion, create `.planning/phases/31-navigation-restructure/31-02-SUMMARY.md`
</output>
