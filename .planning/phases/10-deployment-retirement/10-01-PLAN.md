---
phase: 10-deployment-retirement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/PROJECT.md
autonomous: false

requirements:
  - DEPL-01
  - DEPL-02

must_haves:
  truths:
    - "All documentation references only https://ynab-test-production.up.railway.app — no references to ynab-automation-production.up.railway.app remain in README or PROJECT.md"
    - "The old Railway service (ynab-automation-production) no longer receives or processes any traffic"
  artifacts:
    - path: "README.md"
      provides: "Public-facing documentation with correct deployment URL"
      contains: "ynab-test-production.up.railway.app"
    - path: ".planning/PROJECT.md"
      provides: "Internal project state with correct live URL"
      contains: "ynab-test-production.up.railway.app"
  key_links:
    - from: "README.md"
      to: "https://ynab-test-production.up.railway.app"
      via: "webhook URL example and any live deployment references"
      pattern: "ynab-test-production\\.up\\.railway\\.app"
---

<objective>
Retire the old Railway deployment and update all documentation to reference only the current active instance.

Purpose: The project now has two Railway deployments and the old one (ynab-automation-production) should be decommissioned so it cannot receive traffic. All documentation must point exclusively to the new deployment URL (ynab-test-production.up.railway.app) with no dead links.

Output: Updated README.md and PROJECT.md, old Railway service deleted.
</objective>

<execution_context>
@/data/home/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs to reference new deployment URL only</name>
  <files>README.md, .planning/PROJECT.md</files>
  <action>
    Update all references to the old deployment URL in the two files.

    OLD URL to replace: https://ynab-automation-production.up.railway.app
    NEW URL to use: https://ynab-test-production.up.railway.app

    In README.md:
    - The webhook example URL in step 5 of "Quickest Path" uses a placeholder (`your-app.railway.app`) — leave placeholders as-is, they are generic instructions for users deploying their own instance. Do NOT replace generic `your-app.railway.app` placeholders.
    - There are no hardcoded production URLs in README.md currently (confirmed by grep). No changes needed unless a live URL is found.

    In .planning/PROJECT.md:
    - Line 7: Change `**Live at:** https://ynab-automation-production.up.railway.app` to `**Live at:** https://ynab-test-production.up.railway.app`

    After editing, verify no remaining references to `ynab-automation-production.up.railway.app` exist in README.md or .planning/PROJECT.md.

    Note: The v2.0 milestone file (.planning/milestones/v2.0-REQUIREMENTS.md) contains a historical reference to the old URL in a validation note. Leave this file untouched — it is a historical archive and the reference is part of the completed audit record.
  </action>
  <verify>
    <automated>grep -n "ynab-automation-production" /data/home/ynab/README.md /data/home/ynab/.planning/PROJECT.md; echo "Exit: $? (0 = found references = FAIL, non-zero or no output = PASS)"</automated>
  </verify>
  <done>
    Neither README.md nor .planning/PROJECT.md contains any reference to ynab-automation-production.up.railway.app. PROJECT.md "Live at:" line points to ynab-test-production.up.railway.app.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Decommission old Railway service</name>
  <files>N/A — Railway dashboard action, no local files modified</files>
  <action>
    Delete the old Railway service (ynab-automation-production) from the Railway dashboard. This cannot be done via CLI — it requires navigating the Railway web UI.

    Steps:
    1. Go to https://railway.app/dashboard
    2. Open the project that contains the OLD service named "ynab-automation-production" (distinct from the active "ynab-test-production" project/service)
    3. Click the service, go to Settings, find Danger Zone, click Delete Service, confirm.
    4. If the old deployment is its own separate Railway project, delete the entire project from project settings → Danger Zone → Delete Project.

    After deletion:
    - Verify https://ynab-automation-production.up.railway.app returns a Railway 404 / "service not found" page
    - Verify the new deployment still responds: curl -s -o /dev/null -w "%{http_code}" -X POST https://ynab-test-production.up.railway.app/api/webhook (expect 400 or 200, not a network error)
  </action>
  <verify>curl -s -o /dev/null -w "%{http_code}" -X POST https://ynab-test-production.up.railway.app/api/webhook</verify>
  <done>
    Old Railway service deleted. New deployment at ynab-test-production.up.railway.app responds to webhook requests. No traffic can reach the old service.
  </done>
  <what-built>Task 1 updated the docs. Now the old Railway service needs to be deleted so it no longer receives or processes any traffic.</what-built>
  <how-to-verify>
    Follow the steps in the action above to delete the old service from the Railway dashboard.
  </how-to-verify>
  <resume-signal>Type "done" once the old service has been deleted and you have confirmed the new deployment is still responding.</resume-signal>
</task>

</tasks>

<verification>
After both tasks complete:

1. No references to `ynab-automation-production.up.railway.app` in README.md or PROJECT.md:
   `grep -rn "ynab-automation-production" /data/home/ynab/README.md /data/home/ynab/.planning/PROJECT.md`
   Expected: no output

2. PROJECT.md "Live at:" points to new URL:
   `grep "Live at" /data/home/ynab/.planning/PROJECT.md`
   Expected: `**Live at:** https://ynab-test-production.up.railway.app`

3. Old Railway service is gone (human confirmed in Task 2)

4. New deployment is responding (human confirmed in Task 2)
</verification>

<success_criteria>
- README.md and PROJECT.md reference only https://ynab-test-production.up.railway.app
- No documentation contains dead links to the decommissioned service
- The old Railway service (ynab-automation-production) has been deleted by the user
- The active deployment at ynab-test-production.up.railway.app continues to process webhooks
</success_criteria>

<output>
After completion, create `.planning/phases/10-deployment-retirement/10-01-SUMMARY.md` using the summary template at @/data/home/.claude/get-shit-done/templates/summary.md
</output>
