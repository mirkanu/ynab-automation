---
phase: 03-parse-and-create
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [YNAB-01, YNAB-02]

must_haves:
  truths:
    - "YNAB_BUDGET_ID env var is set on Railway"
    - "YNAB_MANUEL_ACCOUNT_ID env var is set on Railway"
    - "YNAB_EMILY_ACCOUNT_ID env var is set on Railway"
    - "EMILY_KATE_EMAIL env var is set on Railway"
    - "All four env vars are readable by the running Railway service"
  artifacts:
    - path: "Railway environment variables (external)"
      provides: "YNAB routing config and account IDs"
      contains: "YNAB_BUDGET_ID, YNAB_MANUEL_ACCOUNT_ID, YNAB_EMILY_ACCOUNT_ID, EMILY_KATE_EMAIL"
  key_links:
    - from: "YNAB_PERSONAL_ACCESS_TOKEN (existing)"
      to: "https://api.youneedabudget.com/v1/budgets"
      via: "Bearer auth header"
      pattern: "Authorization.*Bearer.*YNAB_PERSONAL_ACCESS_TOKEN"
---

<objective>
Discover YNAB budget and account IDs via the YNAB API, then store them (plus Emily-Kate's email) as Railway environment variables so that Phase 3 implementation can route transactions to the correct accounts.

Purpose: Implementation cannot be written without knowing the YNAB budget ID, Manuel's account ID, and Emily-Kate's account ID. These are runtime values that differ per user and cannot be guessed. Emily-Kate's email address is also unknown and must be confirmed before the routing logic can be written.

Output: Four Railway env vars confirmed: YNAB_BUDGET_ID, YNAB_MANUEL_ACCOUNT_ID, YNAB_EMILY_ACCOUNT_ID, EMILY_KATE_EMAIL.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-parse-and-create/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover YNAB budget and account IDs via API</name>
  <files>None — read-only API calls, no files written</files>
  <action>
    Call the YNAB REST API using the existing YNAB_PERSONAL_ACCESS_TOKEN to enumerate budgets and accounts.

    Run these two curl commands and capture the output:

    Step 1 — List budgets:
    ```
    curl -s -H "Authorization: Bearer $YNAB_PERSONAL_ACCESS_TOKEN" \
      https://api.youneedabudget.com/v1/budgets | jq '.data.budgets[] | {id, name}'
    ```
    Identify the correct budget (the household budget used by Manuel and Emily-Kate). Record its `id` as YNAB_BUDGET_ID.

    Step 2 — List accounts in that budget:
    ```
    curl -s -H "Authorization: Bearer $YNAB_PERSONAL_ACCESS_TOKEN" \
      https://api.youneedabudget.com/v1/budgets/{BUDGET_ID}/accounts | jq '.data.accounts[] | {id, name, type, closed}'
    ```
    Identify Manuel's spending account and Emily-Kate's spending account. Record their `id` values as YNAB_MANUEL_ACCOUNT_ID and YNAB_EMILY_ACCOUNT_ID.

    Print a clear summary:
    - YNAB_BUDGET_ID=...
    - YNAB_MANUEL_ACCOUNT_ID=...
    - YNAB_EMILY_ACCOUNT_ID=...

    Do NOT write any files. Do NOT set the vars yet — that is the checkpoint task.
  </action>
  <verify>
    <automated>curl -sf -H "Authorization: Bearer $YNAB_PERSONAL_ACCESS_TOKEN" https://api.youneedabudget.com/v1/budgets | jq '.data.budgets | length'</automated>
    Returns a number > 0 confirming the token works and budgets are accessible.
  </verify>
  <done>Three IDs identified and printed: YNAB_BUDGET_ID, YNAB_MANUEL_ACCOUNT_ID, YNAB_EMILY_ACCOUNT_ID</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Set env vars on Railway and confirm Emily-Kate's email</name>
  <what-built>Task 1 discovered the YNAB budget ID and account IDs and printed them to the terminal.</what-built>
  <how-to-verify>
    Set all four environment variables on the Railway service (ynab-automation-production):

    1. Open Railway dashboard → ynab-automation-production → Variables tab
    2. Add the following variables using the values printed by Task 1:
       - YNAB_BUDGET_ID = (value from Task 1)
       - YNAB_MANUEL_ACCOUNT_ID = (value from Task 1)
       - YNAB_EMILY_ACCOUNT_ID = (value from Task 1)
    3. Confirm Emily-Kate's email address (the Gmail address she uses to forward Amazon emails)
    4. Add: EMILY_KATE_EMAIL = (her confirmed email address)
    5. Railway will redeploy automatically after saving variables.

    When done, type: "vars set — Emily-Kate email: {her address}"
  </how-to-verify>
  <resume-signal>Type "vars set — Emily-Kate email: {her-email@gmail.com}" with her actual address</resume-signal>
</task>

</tasks>

<verification>
After the checkpoint is complete:
- Railway service has redeployed with the new vars
- curl https://ynab-automation-production.up.railway.app/api/webhook returns 200
- All four vars are visible in Railway Variables tab
</verification>

<success_criteria>
- YNAB_BUDGET_ID, YNAB_MANUEL_ACCOUNT_ID, YNAB_EMILY_ACCOUNT_ID, EMILY_KATE_EMAIL are set on Railway
- The service is running (health check passes)
- Plan 03-02 can now be written and executed with exact account IDs in hand
</success_criteria>

<output>
After completion, create `.planning/phases/03-parse-and-create/03-01-SUMMARY.md`

Include: the four env var names, a note of what budget/account names were found, and Emily-Kate's confirmed email address.
</output>
