---
phase: 26-readme-and-onboarding-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [README-01, README-02, README-03, README-04, README-05]

must_haves:
  truths:
    - "First paragraph explains the YNAB-multi-category-payee problem specifically (not generic 'creates transactions from emails')"
    - "Deploy button opens Railway in a new tab"
    - "Install section has exactly three steps: deploy, find URL, open and set password"
    - "No Steps 2-9 wizard walkthrough text anywhere in the README"
    - "Domain discovery instruction says the URL is auto-generated and tells the user where Railway shows it"
  artifacts:
    - path: "README.md"
      provides: "Updated README with problem statement, simplified install, and corrected deploy button"
      contains: "target=\"_blank\""
  key_links:
    - from: "README.md opening paragraph"
      to: "YNAB multi-category-payee problem"
      via: "specific description of the pain point"
      pattern: "YNAB.*payee|payee.*YNAB|Amazon.*categor|categor.*Amazon"
    - from: "Deploy button"
      to: "Railway template"
      via: "anchor tag with target=\"_blank\""
      pattern: "target=\"_blank\""
---

<objective>
Rewrite README.md to explain the specific problem being solved, simplify the install to three steps, and fix the deploy button to open in a new tab.

Purpose: The current README opens with a generic one-liner and walks through nine wizard steps that are self-explanatory in the UI. A new reader doesn't understand the specific value proposition, and the deploy button doesn't open in a new tab.
Output: README.md with a sharp problem statement, a corrected deploy button, and a three-step install section.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite problem statement opening</name>
  <files>README.md</files>
  <action>
Replace the current opening paragraph (lines 1-5 of README.md):

```
# YNAB Automation

Automatically creates YNAB transactions from forwarded order confirmation emails.
Forward an order confirmation to a dedicated address — a transaction appears in your
budget within seconds, with no manual entry.
```

With a problem-first opening:

```
# YNAB Automation

YNAB automatically categorizes transactions by payee — which works great until you
shop at a place like Amazon, where one order might be groceries, another electronics,
and another home supplies. YNAB sees the same payee every time and can't tell them
apart. You end up manually opening each order confirmation email to figure out what
you actually bought before you can categorize the transaction.

This app eliminates that lookup. Forward your order confirmation emails to a
dedicated address — the app extracts the details and creates a YNAB transaction
automatically, within seconds.
```

Keep the two blockquotes (no-warranty and AI-generated) that follow, unchanged.
  </action>
  <verify>grep -n "YNAB.*payee\|payee.*YNAB\|Amazon.*categor" README.md | head -5</verify>
  <done>Opening paragraph explains the YNAB payee-based categorization problem and how multi-category payees like Amazon break it</done>
</task>

<task type="auto">
  <name>Task 2: Fix deploy button and simplify Install section</name>
  <files>README.md</files>
  <action>
Make three targeted edits to README.md:

**Edit A — Deploy button (README-02):**
Find the current deploy button:
```
[![Deploy on Railway](https://railway.app/button.svg)](https://railway.com/template/bIms_s)
```
Replace with:
```
<a href="https://railway.com/template/bIms_s" target="_blank"><img src="https://railway.app/button.svg" alt="Deploy on Railway"></a>
```

**Edit B — Replace entire Install section (README-03, README-04, README-05):**
Delete everything from `## Install` through the end of the `### Step 9` collapsible block (i.e. the closing `</details>` after the wizard screenshots). This removes Steps 1-9 entirely.

Replace with a new Install section:

```markdown
## Install

### Step 1 — Deploy to Railway

Click the **Deploy on Railway** button above. If you don't have a Railway account, sign up first (GitHub login is fastest). You'll land on the YNAB Automation template preview showing the app and a PostgreSQL database. Click **Deploy Now** — no environment variables to fill in.

Wait for the service to show a green check (2–4 minutes the first time).

### Step 2 — Find your app URL

Once the deploy is green, click the app service name in your Railway project. The public URL is shown at the top of the service page (it looks like `https://something.up.railway.app`). You can also find it under **Settings → Networking**. Copy the URL.

### Step 3 — Open the app and set up

Open the URL in a browser. The app starts on a "set your password" screen — this is the setup wizard. The wizard walks you through connecting your YNAB budget, Anthropic API key, Resend API key, and Pipedream inbound address one step at a time, with direct links to each service at the step where you need it.

**If you get stuck**, the [Troubleshooting](#troubleshooting) section covers the most common first-deploy issues.
```

Keep Steps 10 and 11 (email forwarding and test email) but rename them:
- `### Step 10 — Set Up Email Forwarding` → `### After Setup — Set Up Email Forwarding`
- `### Step 11 — Send a Test Email` → `### After Setup — Send a Test Email`

Remove the Prerequisites table section entirely (between "## What This Is" and "## Install"). The Prerequisites table starts at `## Prerequisites` and ends at `See [Costs](#costs) for what each service charges.` followed by the `---` divider — delete this entire block including the `---` separator.
  </action>
  <verify>grep -n "target=\"_blank\"\|## Install\|### Step 1\|### Step 2\|### Step 3\|### Step 4\|### Step 5\|### Step 6\|### Step 7\|### Step 8\|### Step 9\|### After Setup\|## Prerequisites" README.md</verify>
  <done>
    - Deploy button uses anchor tag with target="_blank"
    - Install section has exactly Steps 1, 2, 3 (deploy, find URL, open and set up)
    - Steps 4-9 (wizard walkthrough) do not exist anywhere in the file
    - Prerequisites table removed
    - Email forwarding and test email steps remain under "After Setup" headers
    - Wizard screenshots collapsible is removed along with Steps 2-9
  </done>
</task>

</tasks>

<verification>
After both tasks:

```bash
# Deploy button opens new tab
grep 'target="_blank"' README.md

# No old-style deploy button
grep -c '^\[\!\[Deploy on Railway\]' README.md  # must return 0

# Problem statement contains payee/Amazon reference
grep -i "payee\|Amazon" README.md | head -3

# No wizard step headings 2-9
grep "### Step [2-9]" README.md  # must return nothing

# Install has exactly three numbered steps
grep "### Step" README.md  # must show only Step 1, Step 2, Step 3

# After Setup sections present
grep "After Setup" README.md

# No Prerequisites heading
grep "## Prerequisites" README.md  # must return nothing
```
</verification>

<success_criteria>
- README opens with a paragraph explaining the YNAB payee-categorization problem specific to multi-category payees like Amazon
- Deploy button is an HTML anchor with target="_blank"
- Install section contains exactly three steps (deploy, find URL, open and configure via wizard)
- No Steps 2-9 wizard walkthrough anywhere in the file
- Domain/URL discovery step correctly explains that Railway auto-generates the URL and where to find it
- Email forwarding and test email instructions remain (under "After Setup" headers)
- All other sections (Features, How It Works, Costs, Troubleshooting, Self-Hosting Note, Contributing, License) are unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/26-readme-and-onboarding-polish/26-01-SUMMARY.md`
</output>
```
