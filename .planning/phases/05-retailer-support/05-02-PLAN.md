---
phase: 05-retailer-support
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/app/api/webhook/route.ts
autonomous: false
requirements:
  - RETAIL-01
  - RETAIL-02
  - RETAIL-03

must_haves:
  truths:
    - "Forwarding an Amazon order confirmation creates a YNAB transaction with payee 'Amazon'"
    - "Forwarding a non-Amazon order confirmation creates a YNAB transaction with the correct retailer name as payee"
    - "A forwarded email that Claude cannot parse as an order confirmation triggers a notification to Manuel"
    - "Non-order emails (no amount, no description) do not silently disappear — parse failure notification fires"
    - "The Amazon-only filter is completely removed; all forwarded emails from known senders reach Claude"
  artifacts:
    - path: "src/app/api/webhook/route.ts"
      provides: "Webhook handler without isFromAmazon filter; threads parsed.retailer to createYnabTransaction"
      contains: "parseOrderEmail"
  key_links:
    - from: "src/app/api/webhook/route.ts"
      to: "src/lib/claude.ts"
      via: "Calls parseOrderEmail(html, senderInfo.name)"
      pattern: "parseOrderEmail"
    - from: "src/app/api/webhook/route.ts"
      to: "src/lib/ynab.ts"
      via: "Passes parsed.retailer as payeeName to createYnabTransaction"
      pattern: "payeeName.*retailer"
---

<objective>
Remove the Amazon-only filter from the webhook handler and wire the new `retailer` and `payeeName` fields through the processing pipeline, so any forwarded order confirmation from any retailer produces a correctly-labeled YNAB transaction.

Purpose: This is the user-visible change — non-Amazon order emails now flow through instead of being blocked.
Output: Updated `webhook/route.ts` with no `isFromAmazon` call; `parseOrderEmail` called instead of `parseAmazonEmail`; `payeeName` passed to `createYnabTransaction`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-retailer-support/05-01-SUMMARY.md

<interfaces>
<!-- Interfaces from plan 01 output — executor uses these directly -->

From src/lib/claude.ts (updated by plan 01):
```typescript
export interface ParsedOrder {
  amount: number;
  description: string;
  retailer: string;   // e.g. "Amazon", "Costco", "Apple"
}
export async function parseOrderEmail(
  html: string,
  senderName: string,
): Promise<ParsedOrder | null>
```

From src/lib/ynab.ts (updated by plan 01):
```typescript
export interface YnabTransactionParams {
  budgetId: string;
  accountId: string;
  amount: number;
  description: string;
  senderName: string;
  payeeName: string;  // dynamic — pass parsed.retailer here
}
```

Current webhook handler (src/app/api/webhook/route.ts) — key sections to change:

Import line (change):
  import { extractMessageId, extractOriginalSender, isFromAmazon } from '@/lib/email';
  import { parseAmazonEmail } from '@/lib/claude';
→ Remove isFromAmazon from email import; change parseAmazonEmail to parseOrderEmail

Step 4 block (DELETE entirely — lines 47-61):
  // Step 4: Filter non-Amazon emails — notify sender
  if (!isFromAmazon(body)) { ... }

Step 5 log message (update):
  'Processing Amazon email from:' → 'Processing order email from:'

Step 7 call (update):
  const parsed = await parseAmazonEmail(html, senderInfo.name);
→ const parsed = await parseOrderEmail(html, senderInfo.name);

  Error notification subject (update):
  'YNAB automation: failed to parse Amazon email' → 'YNAB automation: failed to parse order email'

  Error notification body (update):
  'An Amazon order email forwarded by...' → 'An order confirmation email forwarded by...'

Step 8 createYnabTransaction call (add payeeName):
  await createYnabTransaction({
    budgetId,
    accountId: senderInfo.accountId,
    amount: parsed.amount,
    description: parsed.description,
    senderName: senderInfo.name,
    payeeName: parsed.retailer,   // ADD THIS LINE
  });

Step 8 error notification body (update):
  'An Amazon order email forwarded by...' → 'An order confirmation email forwarded by...'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Amazon filter and wire retailer through webhook handler</name>
  <files>src/app/api/webhook/route.ts</files>
  <action>
    Make the following targeted changes to `src/app/api/webhook/route.ts`:

    1. IMPORTS — Update the import from `@/lib/email`: remove `isFromAmazon` from the destructured list.
       Change the import from `@/lib/claude`: rename `parseAmazonEmail` to `parseOrderEmail`.

    2. DELETE Step 4 block (lines ~47-61): Remove the entire `if (!isFromAmazon(body))` block including its comment header "Step 4: Filter non-Amazon emails — notify sender". This removes the Amazon-only gate.

    3. RE-NUMBER steps: The old Step 5 becomes the new Step 4, Step 6 → Step 5, Step 7 → Step 6, Step 8 → Step 7. Update the comment labels and console.log step references accordingly (e.g. 'Step 5: writing ProcessedEmail record' → 'Step 4: writing ProcessedEmail record').

    4. UPDATE the "Processing" console.log (was Step 5, now Step 4):
       Change `'Processing Amazon email from:'` to `'Processing order email from:'`

    5. UPDATE Step 7 (was Step 7, now Step 6) Claude call:
       Change `parseAmazonEmail(html, senderInfo.name)` to `parseOrderEmail(html, senderInfo.name)`.
       Change error notification subject from `'YNAB automation: failed to parse Amazon email${senderLabel(senderKey)}'`
       to `'YNAB automation: failed to parse order email${senderLabel(senderKey)}'`.
       Change error body text from `'An Amazon order email forwarded by ${sender ?? 'unknown'} could not be parsed...'`
       to `'An order confirmation email forwarded by ${sender ?? 'unknown'} could not be parsed...'`.

    6. UPDATE Step 8 (was Step 8, now Step 7) createYnabTransaction call:
       Add `payeeName: parsed.retailer,` to the params object (after `senderName: senderInfo.name`).
       Change error notification body from `'An Amazon order email forwarded by...'`
       to `'An order confirmation email forwarded by...'`.

    Do not change the `senderLabel` helper, the dedup logic, the sender resolution map, or the unknown-sender notification — those are correct as-is.
  </action>
  <verify>
    <automated>cd /data/home/ynab && npx tsc --noEmit && npm test</automated>
  </verify>
  <done>
    - TypeScript compiles with no errors (no lingering isFromAmazon or parseAmazonEmail references)
    - All tests pass (npm test exits 0)
    - `isFromAmazon` is not imported in route.ts
    - `parseOrderEmail` is called with the email HTML and senderInfo.name
    - `payeeName: parsed.retailer` is in the createYnabTransaction call
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 5 retailer support is fully wired:
    - Amazon filter removed — any retailer's order email now flows through
    - Claude extracts retailer name and it becomes the YNAB payee
    - Parse failures still trigger Manuel notification
  </what-built>
  <how-to-verify>
    Forward a non-Amazon order confirmation email (e.g. Costco, Apple, ASOS, any online retailer) to the Pipedream inbound address (empk1lk0u08wjyn@upload.pipedream.net).

    Expected:
    1. A YNAB transaction is created in the correct account (Manuel's or Emily-Kate's)
    2. The payee_name in YNAB is the retailer's name (e.g. "Costco") — NOT "Amazon"
    3. The memo reads "{Sender}: {description} - Automatically added from email"

    Also verify Amazon still works:
    4. Forward an Amazon order confirmation — transaction appears with payee "Amazon"

    Railway logs are at: https://railway.app/project — check the ynab-automation service logs for "Processing order email from:" to confirm the new code path ran.

    If no non-Amazon order email is handy, forward any email to test the parse-failure path:
    5. Forward a plain non-order email — Manuel should receive a "failed to parse order email" notification
  </how-to-verify>
  <resume-signal>Type "approved" if both Amazon and non-Amazon transactions land correctly, or describe any issues observed.</resume-signal>
</task>

</tasks>

<verification>
TypeScript must compile cleanly and all unit tests must pass before the checkpoint:

```bash
cd /data/home/ynab && npx tsc --noEmit && npm test
```

Confirm no reference to `isFromAmazon` or `parseAmazonEmail` remains in route.ts:

```bash
grep -n "isFromAmazon\|parseAmazonEmail" /data/home/ynab/src/app/api/webhook/route.ts
```

Expected: no output (both are gone from webhook).
</verification>

<success_criteria>
- `isFromAmazon` is not called anywhere in `webhook/route.ts`
- `parseOrderEmail` is called with the HTML body in Step 6
- `payeeName: parsed.retailer` is passed to `createYnabTransaction`
- `npx tsc --noEmit` exits 0
- `npm test` exits 0
- Human verification: non-Amazon order email creates YNAB transaction with correct retailer as payee
</success_criteria>

<output>
After completion, create `.planning/phases/05-retailer-support/05-02-SUMMARY.md`
</output>
