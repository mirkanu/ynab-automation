---
phase: 20-schema-rollback-migration
plan: 03
type: execute
wave: 3
depends_on:
  - "20-01"
  - "20-02"
files_modified: []
autonomous: true
requirements:
  - DATA-05
  - DATA-06
  - DATA-07
  - DATA-08

must_haves:
  truths:
    - "prisma migrate deploy runs without error on Railway"
    - "The six dropped tables no longer exist in the production schema"
    - "The userId column is absent from Setting, ActivityLog, and ProcessedEmail"
    - "pg_policies is empty for our schema after the migration"
    - "Every ActivityLog row present before the migration is still present after (count matches pre-migration snapshot)"
    - "Setting rows (SENDERS, CURRENCY_ACCOUNTS, TEST_MODE) match the pre-migration snapshot exactly"
    - "ProcessedEmail row count matches the pre-migration snapshot"
  artifacts:
    - path: ".planning/phases/20-schema-rollback-migration/post-migration-verification.txt"
      provides: "Post-migration psql query output confirming all success criteria"
  key_links:
    - from: "prisma migrate deploy"
      to: "Railway production DB"
      via: "railway run"
      pattern: "railway run.*prisma migrate deploy"
    - from: "post-migration-verification.txt"
      to: ".planning/phases/20-schema-rollback-migration/pre-migration-snapshot.sql"
      via: "manual diff"
      pattern: "ActivityLog count, Setting rows"
---

<objective>
Deploy the rollback migration to Railway production and verify every success criterion from the Phase 20 ROADMAP against the live database.

Purpose: This plan is the point of no return. It executes Plan 20-02's SQL against production, then runs concrete psql queries to confirm the data-preservation and schema-cleanup goals. The verification queries produce a file that can be diffed against the pre-migration snapshot from Plan 20-01.

Output: A deployed production DB matching the single-tenant schema + `post-migration-verification.txt` file with query outputs confirming all success criteria. The v5.0 app code will be temporarily broken against this schema — that is expected and documented here. Phase 21 must land within 24 hours.

RECOVERY PROCEDURE (if pre-flight check fails or migration fails):
1. Do NOT attempt the migration.
2. Restore from the pg_dump backup: `psql "$DATABASE_URL" < .planning/phases/20-schema-rollback-migration/pre-migration-backup.sql`
3. Fix Plan 20-02 (re-run Task 1 to re-discover constraint names, re-author the SQL in Task 2).
4. Re-run `/gsd:execute-phase 20` from Plan 20-02 onward.
Wave 3 cannot re-execute a Wave 2 plan — if constraint names are wrong at this stage, the only path forward is restore-then-fix.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-schema-rollback-migration/20-01-SUMMARY.md
@.planning/phases/20-schema-rollback-migration/20-02-SUMMARY.md
@.planning/phases/20-schema-rollback-migration/pre-migration-snapshot.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight check, then deploy migration to Railway production</name>
  <files>.planning/phases/20-schema-rollback-migration/post-migration-verification.txt</files>
  <action>
**PRE-FLIGHT STEP A — Confirm backup exists:**
```
ls -la /data/home/ynab/.planning/phases/20-schema-rollback-migration/pre-migration-backup.sql
wc -l /data/home/ynab/.planning/phases/20-schema-rollback-migration/pre-migration-backup.sql
```
If the file is missing or zero bytes, STOP. Do not proceed without the backup.

**PRE-FLIGHT STEP B — Extract all constraint names from migration.sql:**
```
grep -oP 'CONSTRAINT IF EXISTS "\K[^"]+' /data/home/ynab/prisma/migrations/20260410000000_single_tenant_rollback/migration.sql
grep -oP 'INDEX IF EXISTS "\K[^"]+' /data/home/ynab/prisma/migrations/20260410000000_single_tenant_rollback/migration.sql
```
Save this list — these are the names the SQL will attempt to drop.

**PRE-FLIGHT STEP C — Cross-reference against the live DB one final time:**
```
railway run psql "$DATABASE_URL" -c "SELECT conname FROM pg_constraint WHERE conrelid = '\"Setting\"'::regclass AND contype = 'f';"
railway run psql "$DATABASE_URL" -c "SELECT conname FROM pg_constraint WHERE conrelid = '\"ActivityLog\"'::regclass AND contype = 'f';"
railway run psql "$DATABASE_URL" -c "SELECT conname FROM pg_constraint WHERE conrelid = '\"ProcessedEmail\"'::regclass AND contype = 'f';"
railway run psql "$DATABASE_URL" -c "SELECT indexname FROM pg_indexes WHERE schemaname = 'public' AND tablename IN ('Setting', 'ActivityLog', 'ProcessedEmail') ORDER BY tablename, indexname;"
```

For each constraint/index name extracted in Step B, confirm it appears in the live DB output above. If ANY name from the SQL does not match the live DB:
- STOP immediately. Do not run the migration.
- Document the mismatch.
- Follow the recovery procedure in the objective: restore from backup, fix Plan 20-02, re-run `/gsd:execute-phase 20`.

**PRE-FLIGHT STEP D — Confirm no placeholder characters remain:**
```
grep -c '[<>]' /data/home/ynab/prisma/migrations/20260410000000_single_tenant_rollback/migration.sql
```
Expected: `0`. If non-zero, STOP — the SQL still contains template placeholders. Follow recovery procedure.

Only proceed to deployment after Steps A–D all pass.

---

**DEPLOY:**
```
railway run npx prisma migrate deploy
```

Expected output: `The following migration was applied: 20260410000000_single_tenant_rollback`

If migration fails for a reason other than constraint name mismatch (e.g., data error such as duplicate key on the new Setting PK):
1. Read the error message carefully.
2. Investigate with psql to understand the data state.
3. Do NOT attempt a partial restore without user confirmation.
4. Document the error and STOP — the recovery procedure in the objective applies.

After migration succeeds, run all verification queries (Task 2) before declaring success.

DEPLOY FREEZE NOTE: The v5.0 application code references `userId` columns and Auth.js tables that no longer exist after this migration. The app will throw Prisma errors until Phase 21 lands. Do not redeploy the app between this migration and Phase 21 completion. Add a note in the Railway deployment settings if possible. Estimated freeze window: until Phase 21 executes (target: within 24 hours).
  </action>
  <verify>
    <automated>railway run psql "$DATABASE_URL" -c "SELECT migration_name, finished_at FROM _prisma_migrations WHERE migration_name = '20260410000000_single_tenant_rollback';"</automated>
  </verify>
  <done>
    - All four pre-flight checks passed (backup exists, constraint names match live DB, no placeholders)
    - `railway run npx prisma migrate deploy` exits 0
    - `_prisma_migrations` table shows `20260410000000_single_tenant_rollback` with a non-null `finished_at`
  </done>
</task>

<task type="auto">
  <name>Task 2: Run post-migration verification queries and capture output</name>
  <files>.planning/phases/20-schema-rollback-migration/post-migration-verification.txt</files>
  <action>
Run each of the following psql queries against production and capture ALL outputs into `.planning/phases/20-schema-rollback-migration/post-migration-verification.txt`. Every query maps to a ROADMAP success criterion.

**SUCCESS CRITERION 1 — Data preservation: ActivityLog row count matches pre-migration snapshot**
```
railway run psql "$DATABASE_URL" -c "SELECT COUNT(*) as activitylog_count FROM \"ActivityLog\";"
```
Compare to `ACTIVITYLOG ROW COUNT` in `pre-migration-snapshot.sql`. Must match exactly.

**SUCCESS CRITERION 2 — Data preservation: Setting rows match pre-migration snapshot**
```
railway run psql "$DATABASE_URL" -c "SELECT key, value FROM \"Setting\" ORDER BY key;"
```
Compare line by line against Setting rows in `pre-migration-snapshot.sql`. Every key/value pair must be present.

**SUCCESS CRITERION 2b — ProcessedEmail row count**
```
railway run psql "$DATABASE_URL" -c "SELECT COUNT(*) as processedemail_count FROM \"ProcessedEmail\";"
```
Compare to `PROCESSEDEMAIL ROW COUNT` in `pre-migration-snapshot.sql`. Must match exactly.

**SUCCESS CRITERION 3a — Dropped tables: none of the six tables exist**
```
railway run psql "$DATABASE_URL" -c "SELECT tablename FROM pg_tables WHERE schemaname = 'public' ORDER BY tablename;"
```
Expected tables: `ActivityLog`, `ProcessedEmail`, `Setting`, `_prisma_migrations` — and NOTHING ELSE (no User, Account, Session, VerificationToken, EmailForwardingAddress, ProcessedWebhook).

**SUCCESS CRITERION 3b — No userId columns on surviving tables**
```
railway run psql "$DATABASE_URL" -c "SELECT table_name, column_name FROM information_schema.columns WHERE table_schema = 'public' AND column_name = 'userId';"
```
Expected: 0 rows returned.

**SUCCESS CRITERION 3c — No RLS policies remain**
```
railway run psql "$DATABASE_URL" -c "SELECT * FROM pg_policies WHERE schemaname = 'public';"
```
Expected: 0 rows returned.

**SUCCESS CRITERION 3d — Setting PK is key-only**
```
railway run psql "$DATABASE_URL" -c "\d \"Setting\""
```
Expected: No `id` column, no `userId` column. Primary key on `key`.

**SUCCESS CRITERION 3e — ProcessedEmail has messageId-only unique index**
```
railway run psql "$DATABASE_URL" -c "\d \"ProcessedEmail\""
```
Expected: No `userId` column. Unique index `ProcessedEmail_messageId_key` on `messageId`.

Write the output of ALL queries into the verification file with the query label before each result. Then perform the diff against the pre-migration snapshot manually: if ANY count differs or ANY Setting row is missing, document it and STOP — do not mark the plan done.

Commit the verification file to git.
  </action>
  <verify>
    <automated>test -f /data/home/ynab/.planning/phases/20-schema-rollback-migration/post-migration-verification.txt && grep -c "SUCCESS CRITERION" /data/home/ynab/.planning/phases/20-schema-rollback-migration/post-migration-verification.txt</automated>
  </verify>
  <done>
    - All 7 verification queries run and outputs captured in `post-migration-verification.txt`
    - ActivityLog count matches pre-migration snapshot
    - Setting rows match pre-migration snapshot (every key/value pair present)
    - ProcessedEmail count matches pre-migration snapshot
    - `pg_tables` shows only ActivityLog, ProcessedEmail, Setting, _prisma_migrations
    - `pg_policies` returns 0 rows
    - `information_schema.columns WHERE column_name = 'userId'` returns 0 rows
    - Verification file committed to git
  </done>
</task>

</tasks>

<verification>
All 4 ROADMAP success criteria verified by concrete query outputs:

1. ActivityLog data preserved: `SELECT COUNT(*) FROM "ActivityLog"` matches pre-migration snapshot count.
2. Setting rows preserved: `SELECT key, value FROM "Setting" ORDER BY key` matches pre-migration snapshot line-by-line.
3. Schema clean: `SELECT tablename FROM pg_tables WHERE schemaname = 'public'` shows exactly 4 tables (ActivityLog, ProcessedEmail, Setting, _prisma_migrations); `SELECT * FROM pg_policies` returns 0 rows; `SELECT column_name FROM information_schema.columns WHERE column_name = 'userId'` returns 0 rows.
4. Backup exists: `ls -la .planning/phases/20-schema-rollback-migration/pre-migration-backup.sql` shows non-zero file, restore command documented in objective.

Observable final state: `post-migration-verification.txt` file exists, committed, and contains the query outputs for all 7 queries above with zero failures.
</verification>

<success_criteria>
- Pre-flight checks passed: backup exists, all constraint names in SQL match live DB, no template placeholders
- Migration deployed: `_prisma_migrations` shows the new migration with non-null finished_at
- ActivityLog rows preserved: count matches pre-migration snapshot exactly
- Setting rows preserved: all key/value pairs from pre-migration snapshot present post-migration
- ProcessedEmail rows preserved: count matches pre-migration snapshot exactly
- Six tables dropped: pg_tables shows only ActivityLog, ProcessedEmail, Setting, _prisma_migrations
- No userId columns: information_schema query returns 0 rows
- No RLS policies: pg_policies returns 0 rows
- Backup exists and restore command is documented in objective
- post-migration-verification.txt committed to git
- DEPLOY FREEZE acknowledged: Phase 21 must land within 24 hours
</success_criteria>

<output>
After completion, create `.planning/phases/20-schema-rollback-migration/20-03-SUMMARY.md` documenting: migration timestamp, all verification query results (copy from post-migration-verification.txt), confirmation of data preservation (counts match), schema inspection results, and explicit note: "DEPLOY FREEZE in effect — Phase 21 must land before app redeployment or the Prisma client will error on missing tables/columns."
</output>
