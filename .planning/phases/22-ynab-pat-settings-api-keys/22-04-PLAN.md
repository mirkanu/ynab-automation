---
phase: 22-ynab-pat-settings-api-keys
plan: 04
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-02
  - 22-03
files_modified: []
autonomous: false
requirements:
  - YNAB-06
  - YNAB-07
  - YNAB-08
  - YNAB-09
  - CONFIG-01
  - CONFIG-02
  - CONFIG-03
  - CONFIG-04
  - CONFIG-05
  - DASH-06
  - CLEAN-01

must_haves:
  truths:
    - "Admin pastes YNAB PAT into Settings, saves, budget dropdown populates without redeploy"
    - "Selecting a budget populates account dropdown; selections persist across page reload"
    - "API Keys section shows all four editable fields; changing Claude key takes effect on next replay"
    - "Test mode ON + forwarded email → Activity Log entry with no YNAB transaction created"
    - "Sender routing rules and currency routing rules load and save correctly"
    - "Dead-code grep returns zero matches on live deployment"
    - "Forwarded email → YNAB transaction in ≤60s with green Activity Log trace"
  artifacts:
    - path: "https://ynab-test-production.up.railway.app/settings"
      provides: "Live Settings page with YNAB PAT, API Keys, budget/account selection, routing rules"
    - path: "https://ynab-test-production.up.railway.app/logs"
      provides: "Activity Log showing YNAB transaction created trace"
  key_links:
    - from: "Railway deployment"
      to: "YNAB API"
      via: "YNAB_ACCESS_TOKEN from DB Setting via getValidYnabToken()"
      pattern: "verified by budget dropdown populating live"
    - from: "Pipedream webhook"
      to: "/api/webhook"
      via: "POST with email body → YNAB transaction"
      pattern: "Activity Log shows success status"
---

<objective>
Deploy Plans 22-01/02/03 to Railway, run automated smoke tests for all five success criteria, and perform a human-verify checkpoint for the live email forwarding test (criterion 2 requires an actual email to be forwarded).

Purpose: Phase 22 is only complete when verified live on Railway. The email forwarding test cannot be automated — it requires the user to forward a real order confirmation email.
Output: Live deployment on Railway with all Phase 22 success criteria verified. Phase 22 VERIFICATION.md created.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ynab-pat-settings-api-keys/22-01-SUMMARY.md
@.planning/phases/22-ynab-pat-settings-api-keys/22-02-SUMMARY.md
@.planning/phases/22-ynab-pat-settings-api-keys/22-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to Railway and run automated smoke tests</name>
  <files></files>
  <action>
    1. Run `npm run build` locally to confirm exit 0 before deploying.
    2. Deploy to Railway: `railway up --detach` (context: mzv4-Zr5l, project: ynab-test).
    3. Tail logs until deployment completes: `railway logs --tail` — wait for "Ready" or check dashboard.
    4. Run automated smoke tests against https://ynab-test-production.up.railway.app:

    **Test A — Unauthenticated API returns 401:**
    `curl -s -o /dev/null -w "%{http_code}" https://ynab-test-production.up.railway.app/api/ynab/budgets` → 401

    **Test B — YNAB status endpoint:**
    `curl -s https://ynab-test-production.up.railway.app/api/ynab/status` (unauth) → 401

    **Test C — Dead code deleted:**
    `curl -s -o /dev/null -w "%{http_code}" https://ynab-test-production.up.railway.app/api/ynab/authorize` → 404 (route deleted)
    `curl -s -o /dev/null -w "%{http_code}" https://ynab-test-production.up.railway.app/api/email/inbound` → 404

    **Test D — Local dead-code grep:**
    `grep -r "authorize\|oauthToken\|oauthRefreshToken\|ynabEncryption\|mailboxHash\|email-routing" src/` → 0 matches

    **Test E — File absence checks:**
    `test -f src/app/api/ynab/authorize/route.ts && echo EXISTS || echo DELETED` → DELETED
    `test -f src/lib/crypto.ts && echo EXISTS || echo DELETED` → DELETED
    `test -f src/lib/email-routing.ts && echo EXISTS || echo DELETED` → DELETED

    **Test F — Build check:**
    `npm run build` → exit 0
    `npx tsc --noEmit` → 0 errors

    Log results of all 6 test groups with pass/fail.
  </action>
  <verify>
    All 6 test groups pass. Railway deployment shows build succeeded. Live URL responds.
  </verify>
  <done>Railway deployment live. All automated smoke tests pass. Ready for human verification checkpoint.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Human verification — YNAB PAT flow, API keys, test mode, and live email forwarding</name>
  <files></files>
  <action>
    The executor must present the following verification instructions to the user and WAIT for their approval before proceeding.

    Present this to the user:

    ---
    **Phase 22 Human Verification**

    Please verify the following steps at https://ynab-test-production.up.railway.app:

    **Criterion 1 — YNAB PAT and budget/account selection:**
    1. Log in and navigate to Settings.
    2. In the "YNAB Connection" section, paste your YNAB Personal Access Token and click "Save Token".
    3. Confirm the budget dropdown populates with your real YNAB budgets (no page reload needed).
    4. Select your budget. Confirm the account dropdown populates with that budget's accounts.
    5. Select an account and click "Save Budget & Account".
    6. Reload the page. Confirm the budget and account selections are still shown (persisted to DB).

    **Criterion 3 — API Keys section:**
    7. In the "API Keys" section, confirm you see editable fields for: YNAB Personal Access Token, Anthropic Claude API Key, Resend API Key, Pipedream Webhook URL.
    8. Rotate the Anthropic Claude API Key to an invalid value (e.g., "invalid-key-test") and save.
    9. Go to the Activity Log and click "Replay" on a recent email entry.
    10. Confirm the replay trace shows a Claude authentication error (not a successful parse).
    11. Restore the real Anthropic key in the API Keys section and save.

    **Criterion 4 — Test mode and routing rules:**
    12. Toggle Test Mode ON in the Email Processing section. Confirm the toggle turns blue.
    13. Forward a real order confirmation email to your Pipedream webhook address.
    14. Wait up to 60 seconds. Go to Activity Log. Confirm a new entry appears with status "test" (NOT "success") and NO corresponding YNAB transaction.
    15. Toggle Test Mode OFF. Confirm it turns grey.
    16. In Sender Routing Rules, confirm you can add/edit/delete a rule (accounts load from the live dropdown).
    17. In Currency Routing, confirm you can add/edit/delete a rule.

    **Criterion 2 — Live email → YNAB transaction:**
    18. With Test Mode OFF, forward a real order confirmation email to your Pipedream webhook.
    19. Wait up to 60 seconds.
    20. Confirm Activity Log shows a new entry with green "success" status and "YNAB transaction created" in the trace.
    21. Open YNAB and confirm the transaction appears in the selected account.

    ---

    Type "approved" when all steps pass, or describe any issues found.
  </action>
  <verify>
    User types "approved" or provides specific issue details to fix.
  </verify>
  <done>All 5 Phase 22 success criteria verified live on Railway by human. Phase 22 is complete.</done>
</task>

</tasks>

<verification>
All automated smoke tests pass (Task 1).
Human approves all 21 verification steps (Task 2).
Phase 22 VERIFICATION.md created with results.
</verification>

<success_criteria>
1. Admin pastes YNAB PAT → budget dropdown populates live → budget + account selection persists ✓
2. Forwarded email → YNAB transaction in ≤60s → Activity Log green success trace → transaction in YNAB UI ✓
3. API Keys section shows 4 fields; rotating Claude key to invalid → Claude auth error in replay trace ✓
4. Test mode ON + email → Activity Log "test" entry, NO YNAB transaction ✓
5. `grep -r "authorize|oauthToken|oauthRefreshToken|ynabEncryption|mailboxHash|email-routing" src/` → 0 matches ✓
</success_criteria>

<output>
After completion, create:
- `.planning/phases/22-ynab-pat-settings-api-keys/22-04-SUMMARY.md`
- `.planning/phases/22-ynab-pat-settings-api-keys/22-VERIFICATION.md` (full verification report)
</output>
