---
phase: 22-ynab-pat-settings-api-keys
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/ynab/authorize/route.ts
  - src/app/api/ynab/callback/route.ts
  - src/app/api/ynab/disconnect/route.ts
  - src/app/api/ynab/selection/route.ts
  - src/app/api/email/inbound/route.ts
  - src/lib/crypto.ts
  - src/lib/email-routing.ts
autonomous: true
requirements:
  - YNAB-08
  - CLEAN-01

must_haves:
  truths:
    - "No file exists at src/app/api/ynab/authorize/route.ts"
    - "No file exists at src/app/api/ynab/callback/route.ts"
    - "No file exists at src/app/api/ynab/disconnect/route.ts"
    - "No file exists at src/app/api/ynab/selection/route.ts"
    - "No file exists at src/app/api/email/inbound/route.ts (or its parent directory)"
    - "No file exists at src/lib/crypto.ts"
    - "No file exists at src/lib/email-routing.ts"
    - "grep returns zero matches for: authorize|oauthToken|oauthRefreshToken|ynabEncryption|mailboxHash|email-routing in src/"
    - "npm run build exits 0 after deletions (nothing imports the deleted files)"
  artifacts:
    - path: "src/app/api/ynab/"
      provides: "Only budgets/, status/ remain — authorize/, callback/, disconnect/, selection/ are gone"
      contains: "budgets/ status/"
  key_links:
    - from: "grep check"
      to: "zero matches"
      via: "grep -r 'authorize|oauthToken|oauthRefreshToken|ynabEncryption|mailboxHash|email-routing' src/"
      pattern: "zero matches"
---

<objective>
Delete all Phase 17/18 dead code: YNAB OAuth routes (authorize, callback, disconnect, selection), the Phase 18 per-user email inbound route, and the supporting libraries (crypto.ts, email-routing.ts). These were stubbed to 501 in Phase 21 — this plan permanently removes them.

Purpose: CLEAN-01 and YNAB-08 require this code not to exist. Deleting now (Wave 1, parallel with Plan 22-01) means Plan 22-03's Settings UI never references these files.
Output: Seven files/directories deleted. `npm run build` clean. Dead-code grep returns zero.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete YNAB OAuth route files and Phase 18 inbound email route</name>
  <files>
    src/app/api/ynab/authorize/route.ts
    src/app/api/ynab/callback/route.ts
    src/app/api/ynab/disconnect/route.ts
    src/app/api/ynab/selection/route.ts
    src/app/api/email/inbound/route.ts
  </files>
  <action>
    Delete the following files using the file system (rm):

    1. `src/app/api/ynab/authorize/route.ts` — then remove the now-empty `authorize/` directory
    2. `src/app/api/ynab/callback/route.ts` — then remove `callback/`
    3. `src/app/api/ynab/disconnect/route.ts` — then remove `disconnect/`
    4. `src/app/api/ynab/selection/route.ts` — then remove `selection/`
    5. `src/app/api/email/inbound/route.ts` — then remove `inbound/`; if `src/app/api/email/` is now empty, remove it too

    All five files are pure 501 stubs (no other file imports them). Removing the directory keeps Next.js routing clean.

    After deletion, run `grep -r "api/ynab/authorize\|api/ynab/callback\|api/ynab/disconnect\|api/ynab/selection\|api/email/inbound" src/` to confirm zero import references remain.
  </action>
  <verify>
    `test -f src/app/api/ynab/authorize/route.ts && echo EXISTS || echo DELETED` → DELETED
    `test -f src/app/api/ynab/callback/route.ts && echo EXISTS || echo DELETED` → DELETED
    `test -f src/app/api/ynab/disconnect/route.ts && echo EXISTS || echo DELETED` → DELETED
    `test -f src/app/api/ynab/selection/route.ts && echo EXISTS || echo DELETED` → DELETED
    `test -f src/app/api/email/inbound/route.ts && echo EXISTS || echo DELETED` → DELETED
  </verify>
  <done>All five route files deleted. Parent directories cleaned up if empty.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead library files and verify clean build</name>
  <files>
    src/lib/crypto.ts
    src/lib/email-routing.ts
  </files>
  <action>
    1. Delete `src/lib/crypto.ts` — contains YNAB OAuth token encryption helpers (AES-GCM). No current file imports it (Phase 21 removed YNAB OAuth code).
    2. Delete `src/lib/email-routing.ts` — Phase 18 per-user email routing lib. No current file imports it.

    Before deleting, confirm no imports with:
    `grep -r "crypto\|email-routing" src/ --include="*.ts" --include="*.tsx" | grep -v "crypto.ts\|email-routing.ts"` — expect zero or only unrelated matches (e.g. Node crypto stdlib).

    After deleting both files, run:
    - `grep -r "authorize\|oauthToken\|oauthRefreshToken\|ynabEncryption\|mailboxHash\|email-routing" src/` → must return 0 matches
    - `npm run build` → must exit 0

    If build fails due to an unexpected import, trace the import and remove it rather than undoing the deletion.
  </action>
  <verify>
    `test -f src/lib/crypto.ts && echo EXISTS || echo DELETED` → DELETED
    `test -f src/lib/email-routing.ts && echo EXISTS || echo DELETED` → DELETED
    `grep -r "authorize\|oauthToken\|oauthRefreshToken\|ynabEncryption\|mailboxHash\|email-routing" src/` → 0 matches
    `npm run build` → exit 0
  </verify>
  <done>Both library files deleted. Dead-code grep returns zero matches. Build passes. Phase 22 success criterion 5 is now satisfied in code (pending deploy verification in Plan 22-04).</done>
</task>

</tasks>

<verification>
`grep -r "authorize\|oauthToken\|oauthRefreshToken\|ynabEncryption\|mailboxHash\|email-routing" src/` → 0 matches.
`npm run build` → exit 0.
`test -d src/app/api/ynab/authorize` → not found.
`test -d src/app/api/email/inbound` → not found.
`test -f src/lib/crypto.ts` → not found.
`test -f src/lib/email-routing.ts` → not found.
</verification>

<success_criteria>
- 7 dead-code files/directories are gone from disk
- Zero import references to deleted files remain anywhere in src/
- Dead-code grep (success criterion 5 pattern) returns 0 matches
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/22-ynab-pat-settings-api-keys/22-02-SUMMARY.md`
</output>
