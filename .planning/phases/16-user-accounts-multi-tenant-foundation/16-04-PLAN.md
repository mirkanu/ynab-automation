---
phase: 16-user-accounts-multi-tenant-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 16-01
  - 16-02
  - 16-03
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "A new email can be used to sign up, a magic link is received, and clicking it lands on the dashboard"
    - "The session persists after browser close and reopen"
    - "Log out returns to /auth/signin"
    - "No user can access another user's data"
    - "All existing data appears under Manuel's account with no data loss"
---

<objective>
Human verification checkpoint for the complete Phase 16 implementation.

Purpose: AUTH-01 (magic link email delivery) and AUTH-02 (real browser session persistence) cannot be verified automatically — they require a real email inbox and browser interaction. This checkpoint ensures the full user journey works end-to-end before Phase 16 is marked complete.

Output: Human confirmation that signup → magic link → dashboard → logout → session persistence all work in the deployed app.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-user-accounts-multi-tenant-foundation/16-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify full Phase 16 end-to-end in browser</name>
  <action>Human verification — no automated action. See how-to-verify steps below.</action>
  <verify>Human performs steps below and types "approved".</verify>
  <done>All 5 verification steps confirmed passing by the user.</done>
  <what-built>
    Complete Phase 16 implementation:
    - Auth.js v5 with magic link signup via Resend
    - Database-backed sessions with session.user.id wired through
    - Log out via Auth.js signOut()
    - All tables scoped by userId with NOT NULL foreign keys
    - Existing data backfilled to Manuel's user account
    - PostgreSQL RLS enabled on ActivityLog, Setting, ProcessedEmail
    - All API routes require authentication and filter by userId
    - Next.js middleware protects all non-auth routes
    - Isolation tests passing (User B cannot see User A's data)
  </what-built>
  <how-to-verify>
    Before verifying, ensure the app is deployed to Railway (or run locally with `npm run dev`):

    **1. Magic link signup (AUTH-01)**
    - Visit /auth/signin
    - Enter your email address and submit
    - Check your inbox for the "Sign in to YNAB Automation" email
    - Click the magic link in the email
    - Verify you land on the dashboard (/)
    - Expected: dashboard shows YNAB Automation header

    **2. Session persistence (AUTH-02)**
    - While logged in from step 1, close the browser tab completely
    - Open a new browser tab and navigate to /
    - Expected: you are still logged in — dashboard loads without redirect to /auth/signin

    **3. Log out (AUTH-03)**
    - Click the Log out button in the dashboard header
    - Expected: redirected to /auth/signin
    - Navigate to / — Expected: redirected back to /auth/signin (not logged in)

    **4. Existing data visible under your account (DATA-02)**
    - Log in as Manuel (the initial user — email used for the backfill)
    - Navigate to /logs
    - Expected: existing activity log entries are visible (not empty)
    - Expected: all entries show the same data as before the migration

    **5. Tenant isolation — unauthenticated access blocked (DATA-04)**
    - While logged out, visit /api/settings directly in the browser
    - Expected: 401 Unauthorized response (not the settings JSON)

    **6. Optional — two-user isolation**
    - Sign up with a second email address
    - Visit /logs as the second user — Expected: empty (no logs for new user)
    - Sign in as Manuel — verify Manuel's logs are still intact
  </how-to-verify>
  <resume-signal>
    Type "approved" when all 5 steps pass, or describe any issues found so they can be fixed before Phase 16 is marked complete.
  </resume-signal>
</task>

</tasks>

<verification>
Human verification per the steps above.
</verification>

<success_criteria>
- Magic link email received and functional
- Session persists across browser close/reopen
- Log out redirects to /auth/signin
- Existing data visible under Manuel's account
- Unauthenticated API access returns 401
</success_criteria>

<output>
After completion, create `.planning/phases/16-user-accounts-multi-tenant-foundation/16-04-SUMMARY.md`
</output>
