---
phase: 25-self-host-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/LICENSE-AUDIT.md
autonomous: true
requirements:
  - LEGAL-03

must_haves:
  truths:
    - "Anyone reviewing this project can see every runtime dependency's license in one file without running npm commands"
    - "Zero GPL/LGPL/AGPL/MPL/copyleft licenses appear in the runtime dependency tree"
    - "BSD-3-clause and Apache-2.0 attribution requirements are explicitly addressed"
  artifacts:
    - path: "docs/LICENSE-AUDIT.md"
      provides: "Per-dependency runtime license audit with copyleft verification"
      contains: "LICENSE-AUDIT"
  key_links:
    - from: "docs/LICENSE-AUDIT.md"
      to: "package.json dependencies"
      via: "npx license-checker --production --json output"
      pattern: "license-checker"
---

<objective>
Generate and commit `docs/LICENSE-AUDIT.md` enumerating every runtime dependency with its license, confirming zero copyleft in the runtime tree, and noting attribution requirements.

Purpose: LEGAL-03 requires a committed audit file so users and operators of this project can verify no copyleft contamination without running tooling themselves. Runtime deps only (devDependencies are excluded from shipping code).

Output: `docs/LICENSE-AUDIT.md` — introduction, per-package table, copyleft verification statement, attribution notes, and audit timestamp + commit SHA.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-self-host-polish/25-CONTEXT.md

<interfaces>
<!-- Runtime dependencies from package.json (the only ones that matter for copyleft) -->
dependencies:
  "@anthropic-ai/sdk": "^0.20.0"
  "@prisma/client": "^5.13.0"
  "iron-session": "^8.0.4"
  "next": "^14.2.0"
  "react": "^18.3.0"
  "react-dom": "^18.3.0"
  "resend": "^6.9.4"

<!-- devDependencies are NOT shipped to users — exclude from runtime audit -->
devDependencies (reference only, not audited):
  "@types/node", "@types/react", "@types/react-dom",
  "playwright", "prisma", "tsx", "typescript", "vitest"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run license-checker and generate docs/LICENSE-AUDIT.md</name>
  <files>docs/LICENSE-AUDIT.md</files>
  <action>
Run `npx license-checker --production --json` in the project root to get the full per-package license JSON for runtime deps only (`--production` excludes devDependencies).

Then run `npx license-checker --production --summary` to get the aggregate license summary.

Inspect the output:
1. Search for any of these copyleft indicators in the JSON: `GPL`, `LGPL`, `AGPL`, `MPL`, `CDDL`, `EPL`, `SSPL`, `CC-BY-SA`. If any match, investigate the specific package — it may be dual-licensed (safe) or genuinely copyleft (blocker). If genuinely copyleft: stop, do NOT write LICENSE-AUDIT.md, and report the finding.
2. Note any `BSD-3-Clause` or `Apache-2.0` packages. BSD-3 requires retaining copyright notices in redistributions; Apache-2.0 requires NOTICE file in source distributions. For self-hosted app deployments (not binary distribution), the packages' own LICENSE files in node_modules satisfy this requirement — document this explicitly.

Write `docs/LICENSE-AUDIT.md` with the following structure:

```markdown
# Runtime Dependency License Audit

**Project:** YNAB Automation  
**Audit date:** [today's date]  
**Generated by:** `npx license-checker --production --json` (license-checker v[version])  
**Scope:** Runtime dependencies only (`dependencies` in package.json). Dev tools (vitest, playwright, typescript, prisma CLI, tsx) are not audited here as they are not shipped.

## Method

Run `npx license-checker --production --json` from the project root to reproduce this audit. Cross-reference the output against the copyleft list: GPL, LGPL, AGPL, MPL, CDDL, EPL, SSPL, CC-BY-SA.

## Summary

[paste the --summary output here]

## Per-Package Detail

| Package | Version | License | Type |
|---------|---------|---------|------|
[one row per package from the JSON — include transitive deps]

"Type" column: "direct" for packages in package.json dependencies, "transitive" for everything else.

## Copyleft Verification

Checked the full JSON output for: GPL, LGPL, AGPL, MPL, CDDL, EPL, SSPL, CC-BY-SA.

**Result: ZERO copyleft-licensed packages found in the runtime tree.**

## Attribution Requirements

[For each BSD-3-Clause package found:]
- **[package-name] [version]** — BSD-3-Clause — copyright notice retained in node_modules/[package-name]/LICENSE. No redistribution of modified source is planned; standard self-host deployment satisfies BSD-3 requirements.

[For each Apache-2.0 package found:]
- **[package-name] [version]** — Apache-2.0 — NOTICE file (if any) retained in node_modules. Self-hosted deployment does not constitute binary redistribution requiring a separate NOTICE file.

[If no special attribution required:]
- No packages require attribution beyond what is already captured in their node_modules LICENSE files.

## Audit Conclusion

All [N] runtime packages use permissive licenses (MIT, ISC, BSD-2, BSD-3, Apache-2.0, or similar). No GPL, LGPL, AGPL, MPL, or other copyleft licenses are present. This project can be self-hosted, modified, and shared under its MIT license without copyleft obligations.

---
*Audit performed: [date]*  
*Commit SHA at time of audit: [run `git rev-parse HEAD` to get current SHA]*
```

Fill in all placeholders from the actual license-checker output. The per-package table should include ALL packages reported by license-checker (could be 200+), not just the 7 direct deps — transitive deps matter too.
  </action>
  <verify>
    <automated>cd /data/home/ynab && test -f docs/LICENSE-AUDIT.md && echo "EXISTS" && wc -l docs/LICENSE-AUDIT.md && grep -c "GPL\|LGPL\|AGPL\|MPL\|CDDL" docs/LICENSE-AUDIT.md || echo "No copyleft found in audit"</automated>
  </verify>
  <done>
- `docs/LICENSE-AUDIT.md` exists and has content (wc -l > 50)
- File contains the "ZERO copyleft-licensed packages found" statement
- If grep for GPL/LGPL/AGPL/MPL returns 0 (or returns 0 in the Copyleft Verification section — mentions of what was checked are fine, actual findings are not)
- Per-package table covers all packages from license-checker output
- File is committed to git
  </done>
</task>

</tasks>

<verification>
1. `test -f docs/LICENSE-AUDIT.md` exits 0
2. `wc -l docs/LICENSE-AUDIT.md` — at least 50 lines (likely 150+)
3. `grep "ZERO copyleft" docs/LICENSE-AUDIT.md` — returns 1 match
4. `grep "Audit date" docs/LICENSE-AUDIT.md` — returns 1 match (confirms timestamp present)
5. `npx license-checker --production --summary 2>&1 | grep -iE "GPL|LGPL|AGPL|MPL"` — returns nothing (confirming no copyleft at run time)
</verification>

<success_criteria>
- LEGAL-03 satisfied: `docs/LICENSE-AUDIT.md` exists, enumerates every runtime dependency with its license, confirms zero GPL/LGPL/AGPL/MPL/copyleft, notes attribution requirements
- File is committed and visible in the repo
- Any BSD-3 or Apache-2.0 packages have their attribution requirements addressed in the document
</success_criteria>

<output>
After completion, create `.planning/phases/25-self-host-polish/25-02-SUMMARY.md`
</output>
