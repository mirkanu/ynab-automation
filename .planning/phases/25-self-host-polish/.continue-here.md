---
phase: 25-self-host-polish
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-04-12T11:37:01.911Z
---

<current_state>
Phase 25 Plans 25-01/02/03 are COMPLETE and committed. Plan 25-04 (human-verify checkpoint) is in progress — all autonomous pre-work is done, the combined Phase 24-04 + Phase 25-04 checkpoint has been presented to the user. The Railway template is generated and accessible at https://railway.com/template/9Qs37P. The user's Part C dry-run (incognito one-click deploy + README walkthrough + forward a real email) is the only remaining action before v6.0 closes.

Phase 24-04 is ALSO still open — its checkpoint was subsumed by 25-04. Both phases close simultaneously on user approval.
</current_state>

<completed_work>

### Phase 24 (all 4 plans' code is done; only 24-04 checkpoint awaits approval)
- Plan 24-01: Deleted all 20 v5.0 test files, rewrote settings.ts to DB-only, added 6 unit tests. `npm test`: 9 files, 117 tests, 0 failing.
- Plan 24-02: Renamed PIPEDREAM_WEBHOOK_URL → INBOUND_EMAIL across wizard + settings. Captured 7 wizard screenshots via Playwright. Resolved pipedream-cosmetic-field todo.
- Plan 24-03: Created railway.json template + full README rewrite (400 lines). Mermaid diagram fixed. Deploy button URL iterated.
- Plan 24-04: Pre-checks passed. Two bugs auto-fixed during dry-run: (a) Mermaid `\n` syntax → `<br/>` (commit 673a4cf), (b) Deploy button URL + honest install flow (same commit). Checkpoint presented to user but NOT yet approved.

### Phase 25
- Plan 25-01: README gains "No warranty" + "AI-generated" blockquotes at top (lines 7/13, above Deploy button). LICENSE file created from scratch with NOTICE preamble + MIT text. LEGAL-01/02 complete.
- Plan 25-02: `docs/LICENSE-AUDIT.md` audits 81 runtime packages (7 direct + 74 transitive). Zero copyleft. `license-checker` added as devDep. LEGAL-03 complete.
- Plan 25-03: Created `ynab-screenshots` Railway project (renamed to "YNAB Automation"), deployed, seeded dummy data (10 Setting rows + 7 ActivityLog rows), captured 3 feature screenshots (dashboard 40KB, settings 164KB full-page, activity-log 66KB). README Features section added. Wizard screenshots demoted to collapsible `<details>`. POLISH-01 complete.
- Plan 25-04 (in progress):
  - Cleaned up 4 orphaned Postgres services in the screenshot project via GraphQL `serviceDelete`
  - Renamed project to "YNAB Automation" via `projectUpdate` mutation
  - Generated Railway template via `templateGenerate` → code `9Qs37P`
  - Attempted `templatePublish` → **account blocked** from publishing to Railway's marketplace. Unpublished template still works via direct URL (verified via Playwright).
  - Updated README Deploy button URL to `https://railway.com/template/9Qs37P`
  - Rewrote Step 1 install instructions to reflect the real near-one-click flow (template preview → Deploy Now → one IRON_SESSION_SECRET prompt → build → generate domain)
  - Committed + pushed `e862b37`
  - Checkpoint re-presented to user (Part C — incognito dry-run)

</completed_work>

<remaining_work>

### User action (Part C of the checkpoint)
1. Open https://github.com/mirkanu/ynab-automation in incognito
2. Click Deploy on Railway → confirm template preview page titled "YNAB Automation"
3. Click Deploy Now → paste IRON_SESSION_SECRET → deploy
4. Walk wizard steps 1-6 using ONLY the README as guide
5. Forward a real order email → confirm YNAB transaction
6. Scan README for legal blockquotes, Features screenshots, Costs section, Troubleshooting
7. Reply "approved — v6.0 complete" or "issue: <describe>"

### Post-approval (Claude does this)
1. Write 25-04-SUMMARY.md
2. Write 24-04-SUMMARY.md + 24-VERIFICATION.md (carried over)
3. Write 25-VERIFICATION.md
4. Close Phase 24 (mark all plans complete, requirements satisfied)
5. Close Phase 25 (same)
6. Update STATE.md + ROADMAP.md
7. Run `/gsd:complete-milestone v6.0` to archive the milestone and tag the release
8. Delete or note the `ynab-screenshots`/`YNAB Automation` Railway project (no longer needed)

</remaining_work>

<decisions_made>

- Railway template published via GraphQL API (templateGenerate + attempted templatePublish). templatePublish blocked by Railway account restriction → unpublished template still works via direct URL. POLISH-02 is "near-complete" — template functions correctly but is not listed in Railway's public marketplace.
- IRON_SESSION_SECRET cannot be auto-generated in Railway templates for user-added services (only managed services like Postgres get `${{ secret(32) }}` at template-generation). Near-one-click with one prompt is the best we can achieve without Railway publishing access.
- Historical Railway deploy button (commit 57d50c1, v3.0 era) was a placeholder that never actually worked — the template ID `ynab-automation` was never published. Phase 25 is the first time the template has been real.
- Settings screenshot regenerated at fullPage:true (commit ff70fce) because the 800px viewport cropped the page at the API Keys section, hiding YNAB Connection, Sender Rules, Currency Rules, Admin Password.
- Phase 24-04 checkpoint subsumed into 25-04 so user does ONE combined dry-run, not two separate verification sessions.
- Wizard install screenshots kept but demoted to collapsible `<details>` — user said they're self-explanatory and don't sell the features.
- Feature screenshots use dummy data from a disposable Railway project — user's live ynab-test-production was never touched.
- settings.ts process.env stale-state leak fixed in Phase 24-01 (DB-only getSetting, loadDbSettings deleted) — resolved todo moved to done/.
- PIPEDREAM_WEBHOOK_URL renamed to INBOUND_EMAIL across codebase in Phase 24-02 — resolved todo moved to done/.

</decisions_made>

<blockers>
- Railway account blocked from `templatePublish` — the unpublished template works via direct URL but is not publicly listed. User may want to contact Railway support to lift the block if marketplace listing matters. Not a v6.0 blocker — the deploy button works.
</blockers>

<context>
This is the FINAL gate for v6.0. All code, docs, screenshots, and template work is done and pushed to master. The only remaining step is the user's hands-on dry-run: click the button in incognito, walk the wizard using only the README, forward an email, confirm YNAB transaction. Once they say "approved", both Phase 24 and Phase 25 close, and `/gsd:complete-milestone v6.0` archives the milestone.

The ynab-screenshots Railway project (renamed "YNAB Automation", ID 82a5a83c) is the template source. It's still running with dummy data. After v6.0 closes, the user can delete it or keep it as the template source (direct-URL deploys clone from it).

All test suites pass (9 files, 117 tests, 0 failing, 0 skipped). TypeScript clean. All todos resolved (2 pending → done). REQUIREMENTS.md shows LEGAL-01/02/03 complete, POLISH-01 complete, POLISH-02 near-complete (template functions but not marketplace-published).
</context>

<next_action>
On resume: present the Part C checkpoint instructions again (incognito dry-run of the Deploy button + README walkthrough). Wait for user to reply "approved" or "issue: ...". Then write summaries, close both phases, run /gsd:complete-milestone v6.0.
</next_action>
