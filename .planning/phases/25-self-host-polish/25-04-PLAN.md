---
phase: 25-self-host-polish
plan: 04
type: execute
wave: 3
depends_on:
  - 25-01
  - 25-02
  - 25-03
files_modified:
  - README.md
autonomous: false
requirements:
  - POLISH-02

must_haves:
  truths:
    - "The Deploy button URL in README.md resolves to a real published Railway template (railway.com/template/<hash>)"
    - "Clicking the button from a fresh Railway account produces a single-click deploy that auto-provisions PostgreSQL, DATABASE_URL, and IRON_SESSION_SECRET"
    - "The README walkthrough (the Phase 24-04 carry-over) has been completed: user walked from deploy → wizard → dashboard without consulting any file other than README"
    - "Both Phase 24 and Phase 25 success criteria are verified in this combined checkpoint"
    - "v6.0 milestone is ready to close"
  artifacts:
    - path: "README.md"
      provides: "Real railway.com/template/<hash> URL in Deploy button"
      contains: "railway.com/template/"
  key_links:
    - from: "README.md Deploy button"
      to: "railway.com/template/<hash>"
      via: "[![Deploy on Railway](...)](https://railway.com/template/...)"
      pattern: "railway\\.com/template/"
---

<objective>
Combined human-verify checkpoint: (1) user publishes the Railway template via the Railway web UI and provides the resulting template URL, Claude updates the Deploy button and pushes; (2) user does an incognito one-click deploy dry-run to confirm true single-click experience; (3) user walks the README from "click deploy" through "complete wizard" — the paused Phase 24-04 verification carry-over.

Purpose: POLISH-02 (real published template), plus the final gate for both Phase 24 and Phase 25, and the v6.0 milestone sign-off.

Output: README.md updated with real template URL, committed and pushed. Both Phase 24 and Phase 25 formally closed. v6.0 complete.

PHASE 24-04 CARRY-OVER: Phase 24-04 is currently paused at its human-verify checkpoint. This plan subsumes it entirely. The user performs ONE combined dry-run covering both the Phase 24 README walkthrough criteria AND the Phase 25 published template criteria.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claire/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-self-host-polish/25-CONTEXT.md
@.planning/phases/25-self-host-polish/25-01-SUMMARY.md
@.planning/phases/25-self-host-polish/25-02-SUMMARY.md
@.planning/phases/25-self-host-polish/25-03-SUMMARY.md
@.planning/phases/24-test-suite-cleanup-and-self-host-docs/24-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-checks before human checkpoint</name>
  <files></files>
  <action>
Run all automated checks so the human checkpoint focuses on live app verification, not mechanical errors.

1. **npm test (full suite):**
   ```bash
   cd /data/home/ynab && npm test
   ```
   Expected: 0 failing, 0 skipped. If any failures, fix before proceeding.

2. **TypeScript check:**
   ```bash
   npx tsc --noEmit
   ```
   Expected: exits 0.

3. **README structure verification:**
   ```bash
   # Legal paragraphs present (25-01)
   grep -c "No warranty" README.md           # expect 1
   grep -c "AI-generated" README.md          # expect 1

   # Features section present (25-03)
   grep -c "## Features" README.md           # expect 1
   grep -c "feature-dashboard" README.md     # expect 1
   grep -c "feature-settings" README.md      # expect 1
   grep -c "feature-activity-log" README.md  # expect 1

   # Wizard screenshots still present but in details (25-03)
   grep -c "wizard-step-1" README.md         # expect >= 1
   grep -c "<details>" README.md             # expect >= 1

   # Phase 24 checks
   grep -c "docs/images/wizard-step" README.md   # expect >= 6
   grep -E "anthropic.com/pricing|railway.app/pricing|resend.com/pricing" README.md  # expect 3 matches
   ```

4. **Screenshot files present (25-03):**
   ```bash
   ls -lh docs/images/feature-dashboard.png docs/images/feature-settings.png docs/images/feature-activity-log.png
   ```
   Expected: all three files exist, each > 10KB.

5. **License audit committed (25-02):**
   ```bash
   test -f docs/LICENSE-AUDIT.md && echo "EXISTS" && grep "ZERO copyleft" docs/LICENSE-AUDIT.md
   ```

6. **Ensure everything is pushed:**
   ```bash
   git status
   git push
   ```
   Wait for Railway auto-deploy to complete on `ynab-test-production`. Confirm green build.

If any check fails, fix it before presenting the human checkpoint.
  </action>
  <verify>
    <automated>cd /data/home/ynab && npm test 2>&1 | tail -5 && echo "---" && grep -c "No warranty" README.md && grep -c "## Features" README.md && test -f docs/LICENSE-AUDIT.md && echo "audit ok" && ls docs/images/feature-*.png</automated>
  </verify>
  <done>All automated checks pass. git push succeeded. Railway auto-deploy is green on ynab-test-production.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Publish Railway template via web UI and verify one-click deploy</name>
  <files>README.md</files>
  <action>
WHAT IS BUILT: Plans 25-01 through 25-03 delivered: legal paragraphs in README, license audit in docs/, feature screenshots from a disposable Railway project, Features section in README, wizard screenshots demoted to collapsible. The `railway.json` file (from Phase 24-03) declares all required env vars. The `ynab-screenshots` project from Plan 25-03 is deployed — use it as the template source (it's disposable, has Postgres, has the right var keys with dummy values).

USER ACTION REQUIRED: Publish the Railway template via the web UI (CLI cannot do this). Then perform a one-click deploy dry-run and walk the README. Provide the template URL in your resume signal.

CLICK-BY-CLICK RAILWAY TEMPLATE PUBLICATION STEPS:

### Part 1: Publish the Railway Template

1. Go to railway.com and log in.

2. Open the source project — either `ynab-screenshots` (recommended — already deployed, disposable) or `ynab-test-production`.

3. Open Project Settings (gear icon / "Settings" tab in project navigation).

4. Scroll down to find "Create Template" or "Template" section. Click "Create Template" (or "Publish Template").

5. Configure the template:
   - Name: `YNAB Automation`
   - Description: `Automatically creates YNAB budget transactions from forwarded order confirmation emails. Forward an email → transaction appears in seconds.`
   - Icon/Image: leave default or pick a finance icon
   - Services to include: check both the app service AND the PostgreSQL service
   - Environment variables — set behavior for each var:
     - `DATABASE_URL` → "from service" / auto-connected to Postgres
     - `IRON_SESSION_SECRET` → "generate" / "secret" (Railway auto-generates on deploy)
     - `ANTHROPIC_API_KEY` → "required", no default
     - `RESEND_API_KEY` → "required", no default
     - `INBOUND_EMAIL` → "required" or empty (wizard collects it)
     - `YNAB_ACCESS_TOKEN`, `YNAB_BUDGET_ID`, `YNAB_ACCOUNT_ID` → empty / optional (wizard collects)
     - `ADMIN_PASSWORD` → empty (wizard sets this)
     - IMPORTANT: Clear any dummy values (e.g. `ScreenshotDemo123`) — template defaults apply to all future deploys
   - GitHub repo: confirm auto-detected as `mirkanu/ynab-automation`

6. Click Publish / Create. Railway shows the template URL: `https://railway.com/template/AbCdEf`

7. Copy the full template URL.

### Part 2: Verify One-Click Deploy (after Claude updates the button URL — see Task 3)

After Claude pushes the updated README:

8. Open README.md on GitHub.
9. Open an incognito window.
10. Click the "Deploy on Railway" button.
11. Confirm the Railway template preview shows:
    - PostgreSQL service included
    - `IRON_SESSION_SECRET` as auto-generated (no manual input)
    - `DATABASE_URL` connected to PostgreSQL
    - GitHub source: `mirkanu/ynab-automation`
12. (Optional) Complete the deploy and confirm redirect to `/setup/1` on the fresh deployment URL.

### Part 3: README Walkthrough Dry-Run (Phase 24-04 carry-over)

Using the fresh deployment from Part 2 (or any fresh Railway deploy), walk README top-to-bottom using ONLY the README as your guide:

13. No-warranty paragraph — clear and complete?
14. AI-generated disclosure — clear and complete?
15. Features screenshots — match the live app?
16. Deploy → complete wizard → forward email → YNAB transaction appears (or confirm from Part 2 above)
17. Costs section — Railway, Claude, Resend with pricing links? No hard dollar amounts?
18. Troubleshooting — covers the most common failure modes?

Resume signal options:
- "approved — v6.0 complete — template URL: https://railway.com/template/AbCdEf"
- "partial — template URL: https://railway.com/template/AbCdEf — issue: [describe]"
- "blocked — [reason]"
  </action>
  <verify>User provides Railway template URL and approval signal.</verify>
  <done>User has typed "approved — v6.0 complete — template URL: https://railway.com/template/[hash]". Railway template is published and verified via incognito one-click deploy. README walkthrough confirmed.</done>
</task>

<task type="auto">
  <name>Task 3: Update Deploy button URL and push final commit</name>
  <files>README.md</files>
  <action>
The user has provided the Railway template URL (e.g., `https://railway.com/template/AbCdEf`).

1. Update the Deploy button in README.md. Find the current Deploy button badge:
   ```markdown
   [![Deploy on Railway](https://railway.app/button.svg)](...)
   ```
   Replace the link target with the real template URL provided by the user:
   ```markdown
   [![Deploy on Railway](https://railway.app/button.svg)](https://railway.com/template/AbCdEf)
   ```
   Use the actual hash from the user's resume signal, not `AbCdEf`.

2. Remove any remaining "3-click workaround" language. If any honest-disclaimer text about the deploy button requiring manual steps still exists (e.g., "you will need to click Deploy from GitHub repo"), remove it — the template now provides true one-click.

3. Commit and push:
   ```bash
   git add README.md
   git commit -m "feat(25): update Deploy button to real Railway template URL"
   git push
   ```

4. Verify:
   ```bash
   grep "railway.com/template/" README.md
   ```
  </action>
  <verify>
    <automated>cd /data/home/ynab && grep "railway.com/template/" README.md</automated>
  </verify>
  <done>README.md contains `railway.com/template/` in the Deploy button URL. Commit is pushed. Railway auto-deploy triggered on ynab-test-production.</done>
</task>

</tasks>

<verification>
Phase 25 success criteria (all 6 from ROADMAP.md):
1. `grep "No warranty\|personal side project" README.md` — returns matches (LEGAL-01, from 25-01)
2. `grep "AI-generated\|Claude (Anthropic" README.md` — returns matches (LEGAL-02, from 25-01)
3. `test -f docs/LICENSE-AUDIT.md && grep "ZERO copyleft" docs/LICENSE-AUDIT.md` — passes (LEGAL-03, from 25-02)
4. `grep -c "## Features" README.md && ls docs/images/feature-*.png` — Features section + 3 PNGs exist (POLISH-01, from 25-03)
5. `grep -c "<details>" README.md` — wizard screenshots wrapped in collapsible (POLISH-01, from 25-03)
6. `grep "railway.com/template/" README.md` — real template URL in Deploy button (POLISH-02, from this plan)

Phase 24 success criteria (24-04 carry-over):
1. npm test: 0 failing, 0 skipped (Task 1 pre-check)
2. Deploy button → real Railway template with PostgreSQL + auto-IRON_SESSION_SECRET (Part 2 of checkpoint)
3. README walkthrough: deploy → wizard → first email → YNAB transaction (Part 3 of checkpoint)
4. Costs section: 3 providers, pricing links, no hard dollar amounts (Part 3 spot-check)
</verification>

<success_criteria>
POLISH-02 satisfied: "Deploy on Railway" button URL is a real published Railway template (`railway.com/template/<hash>`) producing true one-click deploy with auto-provisioned Postgres, DATABASE_URL, and IRON_SESSION_SECRET — verified by user-led incognito dry-run.

Combined Phase 24 + Phase 25 gate: all Phase 24 ROADMAP.md success criteria and all Phase 25 ROADMAP.md success criteria are satisfied.

v6.0 milestone is verified end-to-end and ready to close.
</success_criteria>

<output>
After completion, create:
1. `.planning/phases/25-self-host-polish/25-04-SUMMARY.md`
2. `.planning/phases/25-self-host-polish/25-VERIFICATION.md` documenting all 6 Phase 25 and all 4 Phase 24 success criteria with verification results

Also create `.planning/phases/24-test-suite-cleanup-and-self-host-docs/24-04-SUMMARY.md` to formally close the paused Phase 24-04 checkpoint — note that verification was carried over to and completed by Phase 25-04.

CLOSURE TRIGGER: On user approval, instruct the orchestrator:
1. Close Phase 24 (mark complete, write 24-VERIFICATION.md)
2. Close Phase 25 (mark complete, write 25-VERIFICATION.md)
3. Run `/gsd:complete-milestone v6.0` to archive and tag the release

Tell the user: "Both Phase 24 and Phase 25 are complete. v6.0 milestone verified. Run `/gsd:complete-milestone v6.0` to archive and tag the release."

Optional cleanup: The `ynab-screenshots` Railway project from Plan 25-03 is no longer needed. Delete it from Railway's web UI (project Settings → Delete Project) to stop hosting costs.
</output>
