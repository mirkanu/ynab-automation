import { describe, it } from 'vitest'
describe('DASH-05: Parse transparency display', () => {
  it.todo('formatParseResult extracts retailer, amount, date, currency from parseResult JSON')
  it.todo('formatParseResult returns null fields when parseResult is null')
  it.todo('formatParseResult does not expose raw token counts or stop_reason debug fields')
  it.todo('formatParseResult handles missing optional fields gracefully')
})
