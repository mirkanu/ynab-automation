import { describe, it } from 'vitest'
describe('DASH-02: Dashboard stats per-user scoping', () => {
  it.todo('getDashboardStats returns only current user stats')
  it.todo('getDashboardStats this-week window is calculated correctly')
  it.todo('getDashboardStats success rate is 0 when no logs exist')
  it.todo('getDashboardStats lastTransaction is null when no logs exist')
})
