import { describe, it } from 'vitest'
describe('ONBD-01: Onboarding flow completion', () => {
  it.todo('new user has onboardingCompleted=false after account creation')
  it.todo('markOnboardingComplete sets onboardingCompleted=true on User row')
  it.todo('user with onboardingCompleted=true is not redirected to /onboarding')
})
